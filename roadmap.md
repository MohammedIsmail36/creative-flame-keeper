# خريطة تنفيذ نظام الفروع المتعددة

المرجع المعتمد: `docs/BRANCH_ACCOUNTING_MODEL.md` + الخطة المؤرشفة في `.lovable/plan/`.

- [x] **المرحلة 0** — وثيقة النموذج المحاسبي (`docs/BRANCH_ACCOUNTING_MODEL.md`): Invariants، محرّك الموازنة، السيناريوهات الثمانية، قواعد التكلفة، معيار النجاح.
- [x] **المرحلة 1** — الهيكل التنظيمي والصلاحيات: `branches`, `warehouses`, `user_branches`, `user_can_access_branch`, `BranchContext`, شاشات الإدارة. (Migration 0008 + بذرة HQ/MAIN + شاشة `/branches` بثلاث تبويبات + محوّل الفرع في القائمة الجانبية — تحقق عملي: إضافة فرع ومخزن بنجاح)
- [x] **المرحلة 2** — بُعد الفرع على سطور القيود + `fn_apply_branch_balancing` + حسابا «مخزون بالطريق 1109» و«تسوية بين الفروع 1110» (الأكواد 1106/1107 مستخدمة مسبقًا، والمرجع الفعلي عبر أدوار في إعدادات الشركة) + `p_branch_id` في `get_account_balances`/`get_ledger_lines` + فلتر الفرع في ميزان المراجعة وقائمة الدخل والميزانية والأستاذ وأرصدة الحسابات + تقرير `/reports/branch-clearing`. (تحقق عملي: قيد سداد مركزي على فرعين ولّد سطري التسوية آليًا، وميزان المراجعة متوازن)
- [x] **المرحلة 3** — (أ) منجز: migration 0011 — warehouse_id/branch_id/quantity_delta/value_delta على الحركات (الفرع مشتق من المخزن)، warehouse_stock + branch_inventory_valuation بتريجرات، منع الصادر فوق المتاح، صفر الكمية=صفر القيمة، rebuild + فحص في سلامة البيانات. (ب) منجز: migration 0012 — warehouse_id على الفواتير والمرتجعات (الحركة تأخذ مخزن المستند)، post_sales_invoice بتكلفة WAC الفرع وقيود بفرع المخزن، مطابقة 1104 لكل فرع في سلامة البيانات، الكمية لكل مخزن في صفحة المنتج، اختيار المخزن في فاتورة البيع. (ج) منجز: خانة المخزن في فاتورة الشراء ومرتجعي البيع والشراء، قيد الشراء على فرع المخزن (0013)، مرتجع الشراء بتكلفة الفرع. ملاحظات مفتوحة: دالة ترحيل مرتجع البيع غير موجودة في القاعدة السحابية (مسبقًا)، فرق تقريب تاريخي 0.02.
- [ ] **المرحلة 4** — الشراء المركزي و`stock_transfers` بخطوتين (نقل داخلي مقابل بين فرعين).
- [ ] **المرحلة 5** — `wallets` بدل قائمة نقد/بنك + إلغاء الشرط المكرر في `payment-voucher.ts` و`expense-posting.ts`.
- [ ] **المرحلة 6** — سلاسل ترقيم لكل فرع، فلاتر وتقارير مقارنة الفروع، إحكام الكتابة، فحوصات سلامة البيانات.

بعد كل مرحلة: فحص الأنواع والاختبارات ← تجربة عملية ← تقرير قصير ← موافقة المستخدم قبل الانتقال.
