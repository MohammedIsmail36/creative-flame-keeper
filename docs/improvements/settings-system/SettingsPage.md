# SettingsPage.tsx — خطة التحسين

**الملف**: `src/pages/SettingsPage.tsx`  
**الأولوية**: 🟢 Low

---

## أخطاء منطقية

- [ ] لا يتم التحقق من حجم/نوع الملف قبل محاولة الرفع — ملف غير صالح يهدر عرض النطاق
- [ ] لا يوجد معالجة لفشل جلب الإعدادات — صفحة فارغة بدون رسالة خطأ
- [ ] لا يتم التحقق من وجود الإعدادات قبل التحديث — قد ينشئ سجلات مكررة
- [ ] **حقل نسبة الضريبة بدون حدود**: يقبل قيم سالبة أو > 100

## مشاكل تجربة المستخدم

- [ ] معاينة الشعار ليست متاحة لذوي الإعاقة — لا يوجد alt text أو وصول بلوحة المفاتيح
- [ ] لا يوجد تأكيد فوري عند رفع الشعار — المستخدم لا يعرف إذا نجح حتى يضغط حفظ
- [ ] حالة التحميل لا تغطي حقول الإدخال — تظهر فارغة ثم تمتلئ (يبدو معطلاً)
- [ ] ثابت `2 * 1024 * 1024` يجب استخراجه لـ `MAX_LOGO_SIZE_MB`

## صيانة وإعادة هيكلة

- [ ] حقول prefix متكررة بنفس النمط (تحرير/عرض) — استخراج لمكوّن `PrefixInput`
- [ ] تنسيق التبويبات يستخدم Tailwind مباشر — استخراج لمكوّن

## اتساق

- [ ] نمط toast الأخطاء مختلف عن باقي الشاشات
- [ ] تنسيق الحقول غير متسق (بعضها `rounded-lg` وبعضها لا)

## التبعيات

- يعتمد على: `_shared-improvements.md` → S8
