# UserManagement.tsx — خطة التحسين

**الملف**: `src/pages/UserManagement.tsx`  
**الأولوية**: 🟢 Low

---

## أخطاء منطقية

- [ ] **لا يتم التحقق من تفرد البريد الإلكتروني** قبل الإنشاء — قد ينشئ مستخدمين بنفس البريد
- [ ] **يمكن للمسؤول خفض رتبة نفسه**: يجب منع تغيير الرتبة الذاتية لتجنب فقدان الوصول
- [ ] **`setTimeout(...fetchUsers(), 1000)` لا ينتظر**: refetch غير مضمون — قد يعرض بيانات قديمة
- [ ] **حذف ملف شخصي بدون فحص ملكية البيانات**: مستخدم لديه فواتير/قيود — بيانات يتيمة

## مشاكل تجربة المستخدم

- [ ] **لا يوجد تأكيد قبل حذف المستخدم**: نقرة واحدة تحذف نهائياً — إضافة AlertDialog
- [ ] زر التصدير يصدّر CSV فقط — يجب إضافة خيار Excel
- [ ] التحقق من الإدخال عند blur فقط — إضافة تحقق لحظي (real-time)
- [ ] نموذج الإضافة لا يُنظّف عند النجاح

## صيانة وإعادة هيكلة

- [ ] `roleLabels`, `roleIcons`, `roleColors` مُعرّفة داخل المكوّن — استخراج لثوابت مشتركة ← S10
- [ ] منطق جلب المستخدمين يربط profiles بـ user_roles يدوياً — يجب أن يكون SQL join في الخلفية
- [ ] Map arrays تُنشأ في كل render — memoize أو استخراج خارج المكوّن

## اتساق

- [ ] نمط Badge variant مختلف عن باقي الشاشات
- [ ] `roleLabels` غير مشترك مع Profile ← S10

## التبعيات

- يعتمد على: `_shared-improvements.md` → S8, S10
