# Profile.tsx — خطة التحسين

**الملف**: `src/pages/Profile.tsx`  
**الأولوية**: 🟢 Low

---

## أخطاء منطقية

- [ ] **سياسة كلمة مرور ضعيفة**: تسمح بـ 6 أحرف بدون متطلبات تعقيد — يجب فرض حد أدنى 8 أحرف + حرف كبير + رقم
- [ ] **Race condition في تسجيل MFA**: إغلاق الحوار قبل اكتمال التحقق يترك حالة غير متسقة — إضافة abort signal
- [ ] **Token/challenge IDs غير مُحققة**: لا يتم التأكد أن challenge للعامل الصحيح — TOCTOU attack
- [ ] البريد الإلكتروني يُعرض بدون التحقق من أنه محدّث — يجب الجلب من auth context

## مشاكل تجربة المستخدم

- [ ] **QR code بدون fallback**: فشل تحميل الصورة يعرض مساحة فارغة — إضافة إدخال يدوي
- [ ] **MFA بدون recovery codes**: تفعيل MFA بدون عرض أكواد استرداد خطير جداً
- [ ] لا يوجد تطابق لحظي لكلمات المرور — الخطأ يظهر فقط عند الإرسال
- [ ] لا يوجد طريقة لإنشاء/تحميل recovery codes

## صيانة وإعادة هيكلة

- [ ] منطق MFA يستخدم عدة state variables (`factorId`, `verifyCode`, `secret`, `qrCode`) — دمج في object state
- [ ] تحقق الحد الأدنى لكلمة المرور مكرر في UI وserver — استخراج لدالة validation مشتركة
- [ ] `roleLabels` مُعرّف محلياً — يجب استيراده من مكان مشترك ← S10

## اتساق

- [ ] `roleLabels` غير مشترك مع UserManagement ← S10
- [ ] حقل الدور يعرض label لكن لا يربط بإدارة الأدوار

## التبعيات

- يعتمد على: `_shared-improvements.md` → S10
