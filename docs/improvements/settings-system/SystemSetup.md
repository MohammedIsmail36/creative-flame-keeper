# SystemSetup.tsx — خطة التحسين

**الملف**: `src/pages/SystemSetup.tsx`  
**الأولوية**: 🟢 Low

---

## أخطاء منطقية

- [ ] **لا يتم إنشاء نسخة احتياطية قبل إعادة التعيين** — حذف كل شيء بلا رجعة
- [ ] **لا يوجد فحص idempotency**: استدعاء `seed-system` مرات متعددة قد ينشئ سجلات مكررة
- [ ] لا يوجد تحقق بالبريد أو OTP قبل العمليات المدمرة — مجرد AlertDialog

## مشاكل تجربة المستخدم

- [ ] نص التحذير كثيف — يجب تقسيمه لنقاط bullet points
- [ ] لا يوجد مؤشر تقدم — العمليات الطويلة تبدو مجمّدة
- [ ] رسائل الخطأ عامة ("Error: undefined") — لا تساعد المستخدم
- [ ] لا يوجد زر نسخ لسجل الأخطاء

## صيانة وإعادة هيكلة

- [ ] نمط try-catch مكرر مرتين بنفس الشكل — استخراج لدالة
- [ ] magic strings: `"seed-system"`, `"database-backup"` — استخراج لثوابت

## اتساق

- [ ] بنية الاستجابة غير متسقة: `data.results` vs `data.error` — توحيد

## التبعيات

- مستقل (لا يعتمد على ملفات أخرى)
