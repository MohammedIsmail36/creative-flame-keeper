# SalesReturnForm.tsx — خطة التحسين

**الملف**: `src/pages/SalesReturnForm.tsx`  
**الأولوية**: 🟠 High

---

## أخطاء حرجة

- [ ] حساب عكس تكلفة المبيعات (COGS reversal) غير متوافق مع طريقة حساب COGS في SalesInvoiceForm — يجب توحيد المنطق
- [ ] لا يتم التحقق من أن الكمية المُرتجعة لا تتجاوز الكمية المباعة أصلاً — يمكن إرجاع أكثر مما بيع
- [ ] لا يوجد دعم معاملات (transactions) — فشل جزئي يترك بيانات غير متسقة

## أخطاء منطقية

- [ ] عند ترحيل المرتجع لا يتم تحديث حالة الفاتورة الأصلية (مثلاً من "مدفوعة" إلى "مرتجعة جزئياً")
- [ ] أكواد الحسابات مكتوبة يدوياً ← S3
- [ ] منطق تحديث كمية المخزون يستخدم fetch ثم update (غير ذري) — race condition ← S6

## مشاكل تجربة المستخدم

- [ ] لا يوجد عرض تفاصيل الفاتورة الأصلية للمقارنة أثناء إنشاء المرتجع
- [ ] لا يوجد مؤشر تحميل أثناء الترحيل
- [ ] لا يوجد تأكيد قبل الترحيل

## صيانة وإعادة هيكلة

- [ ] تسميات الحالات مكررة محلياً ← S1
- [ ] منطق الترحيل مشابه لـ SalesInvoiceForm مع اختلاف الاتجاه — استخراج لدالة مشتركة مع parameter لتحديد الاتجاه
- [ ] تكرار كبير مع PurchaseReturnForm — يمكن استخراج مكوّن ReturnForm مشترك

## اتساق

- [ ] يجب أن يستخدم نفس نمط حساب صافي البند المستخدم في SalesInvoiceForm
- [ ] نمط معالجة الأخطاء مختلف

## التبعيات

- يعتمد على: `_shared-improvements.md` → S1, S3, S4, S6
- يعتمد على: SalesInvoiceForm (توحيد COGS)
