# SalesInvoiceForm.tsx — خطة التحسين

**الملف**: `src/pages/SalesInvoiceForm.tsx`  
**الأولوية**: 🟠 High

---

## أخطاء حرجة

- [ ] دالة `postInvoice()` تحتوي على 138 سطر من منطق الأعمال مختلط مع UI — استخراج إلى `src/lib/invoice-utils.ts` أو `journal-utils.ts`
- [ ] التحقق من الصحة (validation) يتم فقط عند الترحيل وليس عند الحفظ — المستخدم قد يحفظ فاتورة بلا بنود ثم ينسى
- [ ] لا يوجد حالة تحميل/حفظ (loading/saving state) — المستخدم قد يضغط الزر مرتين مما يُنشئ فواتير مكررة
- [ ] حساب تكلفة المبيعات (COGS) غير متسق مع SalesReturnForm — الاسترداد لن يتوازن

## أخطاء منطقية

- [ ] منطق وضع الخصم (نسبة vs مبلغ) غير متسق مع PurchaseInvoiceForm — توحيد السلوك
- [ ] لا يتم التحقق من أن الكمية المتاحة كافية قبل الترحيل — يمكن بيع أكثر من المتاح
- [ ] أكواد الحسابات مكتوبة نصياً (`"1104"`, `"5201"`, `"4201"`) — استبدال بثوابت ← يعتمد على S3
- [ ] لا يتم استخدام transaction — إذا نجح إدراج الفاتورة وفشل القيد المحاسبي تبقى بيانات يتيمة

## مشاكل تجربة المستخدم

- [ ] عند إضافة منتج جديد للبنود، لا يتم تعبئة السعر تلقائياً من آخر سعر بيع
- [ ] لا يوجد عرض ملخص قبل الترحيل — المستخدم لا يرى التأثير المحاسبي
- [ ] لا يوجد تأكيد قبل الترحيل — عملية لا رجعة فيها بدون تأكيد

## صيانة وإعادة هيكلة

- [ ] استخراج منطق الترحيل (postInvoice) إلى `src/lib/invoice-posting.ts` مع unit tests
- [ ] استخراج حساب الإجماليات (totals) إلى دالة مساعدة قابلة للاختبار
- [ ] تسميات الحالات مكررة محلياً ← S1

## اتساق

- [ ] فحص الصلاحيات يتحقق من دور "sales" — لكن Purchases لا يتحقق من أي دور (عدم اتساق)
- [ ] نمط معالجة الخطأ مختلف عن الشاشات الأخرى

## التبعيات

- يعتمد على: `_shared-improvements.md` → S1, S3, S4, S5, S6
- يحجب: SalesReturnForm (يجب توحيد حساب COGS أولاً)
