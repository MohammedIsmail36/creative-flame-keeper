# Sales.tsx — خطة التحسين

**الملف**: `src/pages/Sales.tsx`  
**الأولوية**: 🟠 High

---

## أخطاء منطقية

- [ ] لا يوجد معالجة أخطاء (error handling) عند فشل جلب البيانات — الشاشة تعرض بيانات فارغة بدون إشعار المستخدم
- [ ] فلترة التاريخ تستخدم مقارنة نصية (string comparison) وهي هشّة مع تنسيقات مختلفة — استبدال بمقارنة تاريخ حقيقية
- [ ] لا يتم التحقق من أن `invoice_date <= due_date` عند العرض — قد يظهر فواتير بتواريخ غير منطقية بدون تنبيه

## مشاكل تجربة المستخدم

- [ ] لا توجد حالة فارغة (empty state) عندما لا توجد فواتير — إضافة رسالة توضيحية مع زر إنشاء
- [ ] لا يوجد مؤشر تحميل أثناء الفلترة — المستخدم لا يعرف إذا كان النظام يعمل
- [ ] لا يوجد تأكيد قبل حذف فاتورة مسودة — حذف مباشر بدون تراجع

## صيانة وإعادة هيكلة

- [ ] تسميات الحالات (`draft`, `posted`, إلخ) وألوانها مكررة محلياً — استبدال بـ `INVOICE_STATUS_LABELS` و `INVOICE_STATUS_COLORS` من `constants.ts` ← يعتمد على: `_shared-improvements.md` → S1
- [ ] أنماط فلترة التاريخ مكررة مع Purchases, SalesReturns, PurchaseReturns — استخراج hook مشترك `useDateRangeFilter`

## اتساق

- [ ] عدم اتساق في أسماء الأعمدة مع الشاشات المشابهة (Purchases) — توحيد
- [ ] يستخدم نمط عرض مختلف عن Purchases للجدول — توحيد المظهر

## التبعيات

- يعتمد على: `_shared-improvements.md` → S1 (ثوابت الحالات)، S7 (التواريخ)، S8 (معالجة الأخطاء)
