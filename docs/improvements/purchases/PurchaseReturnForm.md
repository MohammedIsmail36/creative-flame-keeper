# PurchaseReturnForm.tsx — خطة التحسين

**الملف**: `src/pages/PurchaseReturnForm.tsx`  
**الأولوية**: 🟠 High

---

## أخطاء حرجة

- [ ] حساب عكس COGS غير متوافق مع PurchaseInvoiceForm — يجب توحيد المنطق
- [ ] لا يتم التحقق من أن الكمية المُرتجعة لا تتجاوز الكمية المشتراة أصلاً
- [ ] لا يوجد دعم معاملات (transactions) — فشل جزئي يترك بيانات غير متسقة

## أخطاء منطقية

- [ ] عند ترحيل المرتجع لا يتم تحديث حالة فاتورة الشراء الأصلية
- [ ] أكواد الحسابات مكتوبة يدوياً ← S3
- [ ] منطق تحديث كمية المخزون غير ذري (fetch ثم update) — race condition ← S6

## مشاكل تجربة المستخدم

- [ ] لا يوجد عرض تفاصيل فاتورة الشراء الأصلية للمقارنة
- [ ] لا يوجد مؤشر تحميل أثناء الترحيل
- [ ] لا يوجد تأكيد قبل الترحيل

## صيانة وإعادة هيكلة

- [ ] تسميات الحالات مكررة ← S1
- [ ] تكرار كبير مع SalesReturnForm — استخراج مكوّن ReturnForm مشترك
- [ ] منطق الترحيل مشابه لـ PurchaseInvoiceForm — استخراج لدالة مشتركة

## اتساق

- [ ] يجب أن يستخدم نفس نمط حساب صافي البند المستخدم في PurchaseInvoiceForm
- [ ] نمط معالجة الأخطاء مختلف

## التبعيات

- يعتمد على: `_shared-improvements.md` → S1, S3, S4, S6
- يعتمد على: PurchaseInvoiceForm (توحيد COGS)
