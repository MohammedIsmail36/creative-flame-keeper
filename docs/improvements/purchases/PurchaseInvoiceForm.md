# PurchaseInvoiceForm.tsx — خطة التحسين

**الملف**: `src/pages/PurchaseInvoiceForm.tsx`  
**الأولوية**: 🟠 High

---

## أخطاء حرجة

- [ ] **قسمة على صفر**: `itemNetTotal / item.quantity` قد تُرجع `NaN` أو `Infinity` إذا كانت الكمية صفر — إضافة حراسة `if (quantity === 0)`
- [ ] لا يوجد دعم معاملات (transactions) — فشل جزئي يترك بيانات يتيمة
- [ ] لا يوجد حالة حفظ (saving state) — ضغط مزدوج ينشئ فواتير مكررة

## أخطاء منطقية

- [ ] منطق وضع الخصم (نسبة vs مبلغ) غير متسق مع SalesInvoiceForm — يجب توحيد السلوك
- [ ] التحقق من الصحة يتم عند الترحيل فقط وليس عند الحفظ
- [ ] أكواد الحسابات مكتوبة يدوياً ← S3
- [ ] منطق الترحيل مكرر ومشابه لـ SalesInvoiceForm مع اختلاف الاتجاه

## مشاكل تجربة المستخدم

- [ ] لا يوجد عرض ملخص قبل الترحيل
- [ ] لا يوجد تأكيد قبل الترحيل
- [ ] لا يوجد مؤشر تحميل أثناء الحفظ/الترحيل

## صيانة وإعادة هيكلة

- [ ] تسميات الحالات مكررة ← S1
- [ ] استخراج منطق الترحيل لدالة مشتركة مع SalesInvoiceForm ← S4
- [ ] تكرار كبير مع SalesInvoiceForm — يمكن استخراج مكوّن InvoiceForm مشترك

## اتساق

- [ ] لا يتحقق من الصلاحيات على عكس SalesInvoiceForm
- [ ] نمط معالجة الأخطاء مختلف

## التبعيات

- يعتمد على: `_shared-improvements.md` → S1, S3, S4, S5, S6
- يحجب: PurchaseReturnForm (توحيد COGS)
