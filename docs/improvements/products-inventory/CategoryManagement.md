# CategoryManagement.tsx — خطة التحسين

**الملف**: `src/pages/CategoryManagement.tsx`  
**الأولوية**: 🟡 Medium

---

## أخطاء منطقية

- [ ] **فحص الدورات غير مباشر ناقص**: يمنع فقط التعيين الذاتي المباشر (A→A) لكن لا يكشف الدورات غير المباشرة (A→B→C→A) — قد يسبب حلقة لا نهائية عند بناء الشجرة
  - **الإصلاح**: إضافة دالة `hasCircularReference(categoryId, parentId, categories)` تتبع السلسلة كاملة
- [ ] منطق بناء شجرة الأصناف مكرر مع Products ← S12

## مشاكل تجربة المستخدم

- [ ] لا يوجد سحب وإفلات (drag & drop) لتغيير ترتيب/أب الأصناف
- [ ] لا يوجد تأكيد حذف مع عرض عدد المنتجات المتأثرة
- [ ] لا يوجد بحث سريع في الشجرة

## صيانة وإعادة هيكلة

- [ ] استخراج منطق بناء الشجرة ← S12
- [ ] استخراج مكوّن عرض العقدة (TreeNode) لملف منفصل

## اتساق

- [ ] توحيد نمط التأكيد قبل الحذف مع باقي الشاشات

## التبعيات

- يعتمد على: `_shared-improvements.md` → S12
