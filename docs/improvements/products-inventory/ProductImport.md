# ProductImport.tsx — خطة التحسين

**الملف**: `src/pages/ProductImport.tsx`  
**الأولوية**: 🔴 Critical

---

## أخطاء حرجة

- [ ] 🔴 **Race condition في إنشاء الأصناف**: إذا تضمن الملف عدة منتجات لنفس الصنف الجديد، قد يُنشأ الصنف مرتين بسبب عمليات متزامنة
  - **الإصلاح**: إنشاء كل الأصناف الجديدة أولاً في خطوة مسبقة قبل استيراد المنتجات
- [ ] 🔴 **لا يوجد تحقق من الملف**: لا يتم فحص نوع الملف أو حجمه أو ترميزه قبل البدء — ملف تالف قد يسبب خطأ غير مفهوم
- [ ] 🔴 **لا يوجد معاينة قبل الاستيراد**: يتم الاستيراد مباشرة بدون عرض البيانات للمستخدم للمراجعة
- [ ] لا يوجد تراجع عند فشل جزئي — بعض المنتجات تُستورد والبعض لا ← S6

## أخطاء منطقية

- [ ] لا يتم التحقق من تكرار أكواد المنتجات في الملف أو مع الموجود في قاعدة البيانات
- [ ] أكواد الحسابات مكتوبة يدوياً ← S3
- [ ] لا يتم التحقق من صحة القيم الرقمية (كميات سالبة، أسعار = 0)

## مشاكل تجربة المستخدم

- [ ] إضافة شاشة معاينة (preview) تعرض البيانات المُحللة مع إبراز الأخطاء قبل التأكيد
- [ ] إضافة شريط تقدم (progress bar) يوضح عدد المنتجات المُستوردة من الإجمالي
- [ ] إضافة تقرير نتائج بعد الانتهاء يوضح: عدد الناجح، الفاشل، المتجاهل مع الأسباب
- [ ] إضافة نموذج ملف CSV للتحميل

## صيانة وإعادة هيكلة

- [ ] استخراج منطق تحليل CSV إلى `src/lib/csv-import.ts` مع unit tests
- [ ] استخراج منطق التحقق من الصحة لدالة منفصلة

## اتساق

- [ ] يجب أن يستخدم نفس منطق إنشاء المنتج المستخدم في ProductForm

## التبعيات

- يعتمد على: `_shared-improvements.md` → S3, S6, S12
