# ProductForm.tsx — خطة التحسين

**الملف**: `src/pages/ProductForm.tsx`  
**الأولوية**: 🔴 Critical

---

## أخطاء حرجة

- [ ] 🔴 **Race condition عند تحديث الكميات**: يتم جلب الكمية الحالية (fetch) ثم تحديثها (update) — عملية غير ذرية. إذا حفظ مستخدمان في نفس الوقت ستُفقد إحدى التحديثات
  - **الإصلاح**: استخدام `rpc` مع `UPDATE ... SET quantity = quantity + $delta` بدلاً من fetch+update
- [ ] 🔴 **لا يوجد تراجع عند فشل جزئي**: إذا نجح إنشاء المنتج وفشل إنشاء حركة المخزون أو القيد المحاسبي، يبقى المنتج بدون حركة — بيانات غير متسقة ← S6

## أخطاء منطقية

- [ ] أكواد الحسابات مكتوبة يدوياً (`"1104"`, `"5201"`, إلخ) ← S3
- [ ] لا يتم التحقق من أن سعر التكلفة > 0 عند إنشاء حركة شراء
- [ ] منطق حساب القيمة الإجمالية للمخزون عند الرصيد الافتتاحي قد يكون غير دقيق

## مشاكل تجربة المستخدم

- [ ] لا يوجد مؤشر تحميل أثناء الحفظ — ضغط مزدوج ينشئ منتجات مكررة
- [ ] لا يوجد تأكيد قبل مغادرة الصفحة مع تغييرات غير محفوظة
- [ ] لا يوجد معاينة للصورة قبل الرفع

## صيانة وإعادة هيكلة

- [ ] استخراج منطق إنشاء القيد المحاسبي ← S4
- [ ] استخراج منطق إنشاء حركة المخزون لدالة مساعدة

## اتساق

- [ ] يجب أن يستخدم نمط القيد المحاسبي الموحد ← S4

## التبعيات

- يعتمد على: `_shared-improvements.md` → S3, S4, S6
