# CustomerPayments.tsx — خطة التحسين

**الملف**: `src/pages/CustomerPayments.tsx`  
**الأولوية**: 🔴 Critical

---

## أخطاء حرجة

- [ ] 🔴 **تعديل + ترحيل ينشئ دفعة مكررة** (سطر ~331-337): عند تحرير دفعة مسودة ثم ترحيلها، يتم إنشاء سجل جديد بدلاً من تحديث الموجود — ينتج دفعة مكررة في قاعدة البيانات
  - **الإصلاح**: فصل منطق الحفظ عن منطق الترحيل — حفظ أولاً (update) ثم ترحيل (update status + create journal)
- [ ] 🔴 **Race condition في posted_number** (سطر ~340-350): استخدام متزامن يمكن أن يحصل على نفس الرقم لدفعتين — استخدام RPC آمن ← S5
- [ ] لا يوجد دعم معاملات — إذا نجح إنشاء الدفعة وفشل القيد المحاسبي تبقى دفعة بدون قيد

## أخطاء منطقية

- [ ] استعلام كشف المبالغ المستردة (refund detection) يعاني من مشكلة N+1 — استعلام لكل دفعة بدلاً من استعلام واحد
- [ ] منطق إنشاء القيد المحاسبي مكرر مع SupplierPayments ← S4
- [ ] أكواد الحسابات مكتوبة يدوياً ← S3

## مشاكل تجربة المستخدم

- [ ] لا يوجد تحذير عند محاولة دفع مبلغ يتجاوز رصيد العميل المستحق
- [ ] لا يوجد مؤشر تحميل أثناء الترحيل
- [ ] لا يوجد تأكيد قبل الترحيل

## صيانة وإعادة هيكلة

- [ ] تكرار كود هائل مع SupplierPayments — يمكن استخراج مكوّن `PaymentPage` مشترك مع parameter لتحديد النوع (عميل/مورد)
- [ ] استخراج منطق الترحيل لدالة مشتركة ← S4
- [ ] تسميات الحالات مكررة ← S1

## اتساق

- [ ] يتحقق من دور "sales" — يجب أن يكون متسقاً مع نظام الصلاحيات العام
- [ ] نمط معالجة الأخطاء مختلف عن باقي الشاشات

## التبعيات

- يعتمد على: `_shared-improvements.md` → S1, S3, S4, S5, S6
- يحجب: لا شيء (يمكن إصلاحه مستقلاً)
