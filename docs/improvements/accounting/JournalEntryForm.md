# JournalEntryForm.tsx — خطة التحسين

**الملف**: `src/pages/JournalEntryForm.tsx`  
**الأولوية**: 🟠 High

---

## أخطاء حرجة

- [ ] منطق إنشاء القيد المحاسبي مكرر في 4+ ملفات (هنا + Expenses + ExpenseForm + FiscalYearClosing + ...) ← S4
- [ ] التحقق من التوازن يستخدم `Math.abs(debit - credit) < 0.01` مكتوب يدوياً ← S11

## أخطاء منطقية

- [ ] لا يوجد تحقق من أن عدد البنود >= 2 — يمكن إنشاء قيد ببند واحد (غير محاسبي)
- [ ] لا يوجد تحقق من أن كل بند له حساب مختلف عن البنود الأخرى (حساب أخر)
- [ ] `posted_number` يُحسب بـ `MAX + 1` غير آمن ← S5

## مشاكل تجربة المستخدم

- [ ] لا يوجد اقتراح تلقائي للطرف المقابل (عند إدخال مدين يقترح الدائن)
- [ ] لا يوجد مؤشر تحميل أثناء الحفظ
- [ ] لا يوجد تأكيد قبل مغادرة الصفحة مع تغييرات غير محفوظة
- [ ] لا يعرض الفرق بين المدين والدائن بشكل حي أثناء الإدخال

## صيانة وإعادة هيكلة

- [ ] استخراج كل منطق إنشاء القيد لـ `journal-utils.ts` ← S4
- [ ] استخراج التحقق من التوازن ← S11
- [ ] استخراج حساب `posted_number` ← S5

## اتساق

- [ ] يجب أن يكون نمط إنشاء القيد هو المرجع لكل الملفات الأخرى

## التبعيات

- يعتمد على: `_shared-improvements.md` → S4, S5, S11
- يحجب: كل الملفات التي تنشئ قيوداً (يجب استخراج S4 أولاً)
