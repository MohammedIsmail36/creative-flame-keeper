# Accounts.tsx — خطة التحسين

**الملف**: `src/pages/Accounts.tsx`  
**الأولوية**: 🟡 Medium

---

## أخطاء منطقية

- [ ] استعلام الحسابات الطرفية (`is_parent = false`) مكرر في 6+ ملفات — استخراج لدالة مشتركة
- [ ] تطبيع نوع الحساب (`assets`→`asset`) يتم يدوياً في عدة أماكن — توحيد

## مشاكل تجربة المستخدم

- [ ] لا يوجد فلتر حسب نوع الحساب (أصول، خصوم، إيرادات، مصروفات)
- [ ] لا يوجد عرض شجري للحسابات الأب والفرعية
- [ ] لا يوجد تأكيد قبل حذف حساب مع عرض عدد القيود المرتبطة

## صيانة وإعادة هيكلة

- [ ] استخراج helper `fetchLeafAccounts()` يعيد فقط الحسابات الطرفية
- [ ] استخراج helper `normalizeAccountType()` لتوحيد أسماء الأنواع

## اتساق

- [ ] توحيد نمط البحث والفلترة مع باقي شاشات القوائم

## التبعيات

- يعتمد على: `_shared-improvements.md` → S8
