# TrialBalance.tsx — خطة التحسين

**الملف**: `src/pages/TrialBalance.tsx`  
**الأولوية**: 🟡 Medium

---

## أخطاء منطقية

- [ ] تسامح النقطة العائمة يستخدم `0.01` مكتوب يدوياً — يجب استخدام ثابت مشترك ← S11
- [ ] كشف قيد الإقفال يستخدم مطابقة نصية هشة: `.like("description", "%قيد إقفال السنة المالية%")` — يجب استخدام حقل `reference_type` المهيكل
- [ ] تطبيع نوع الحساب (`assets`→`asset`) يتم يدوياً — توحيد

## مشاكل تجربة المستخدم

- [ ] لا يوجد فلتر حسب نوع الحساب
- [ ] لا يظهر تنبيه واضح عندما يكون ميزان المراجعة غير متوازن
- [ ] لا يوجد مقارنة مع فترات سابقة

## صيانة وإعادة هيكلة

- [ ] استخراج التحقق من التوازن ← S11
- [ ] استخراج كشف قيود الإقفال لدالة مشتركة مع BalanceSheet و IncomeStatement
- [ ] استخراج تطبيع أنواع الحسابات لدالة مشتركة

## اتساق

- [ ] منطق كشف قيود الإقفال يجب أن يكون متطابقاً مع BalanceSheet و IncomeStatement
- [ ] نمط عرض المبالغ يجب أن يكون متسقاً

## التبعيات

- يعتمد على: `_shared-improvements.md` → S11
