# IncomeStatement.tsx — خطة التحسين

**الملف**: `src/pages/IncomeStatement.tsx`  
**الأولوية**: 🟡 Medium

---

## أخطاء منطقية

- [ ] **استبعاد قيود الإقفال غير متسق مع BalanceSheet**: يستخدم نمط مطابقة نصية مختلف — قد يستبعد/يشمل قيوداً مختلفة
  - **الإصلاح**: توحيد منطق كشف قيود الإقفال في دالة مشتركة
- [ ] تسامح النقطة العائمة مكتوب يدوياً ← S11
- [ ] تطبيع نوع الحساب يتم يدوياً

## مشاكل تجربة المستخدم

- [ ] لا يوجد عرض مقارن (فترة حالية vs فترة سابقة)
- [ ] لا يوجد حساب نسب (هامش ربح إجمالي، هامش ربح صافي)
- [ ] لا يوجد تصدير مباشر

## صيانة وإعادة هيكلة

- [ ] توحيد كشف قيود الإقفال مع TrialBalance و BalanceSheet
- [ ] استخراج حساب الإيرادات والمصروفات لدوال قابلة للاختبار

## اتساق

- [ ] يجب أن يستخدم نفس نمط كشف قيود الإقفال
- [ ] يجب أن يستخدم نفس تطبيع أنواع الحسابات

## التبعيات

- يعتمد على: `_shared-improvements.md` → S11
- مرتبط بـ: TrialBalance, BalanceSheet
