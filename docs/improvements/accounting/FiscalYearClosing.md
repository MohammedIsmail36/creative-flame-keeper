# FiscalYearClosing.tsx — خطة التحسين

**الملف**: `src/pages/FiscalYearClosing.tsx`  
**الأولوية**: 🔴 Critical

---

## أخطاء حرجة

- [ ] 🔴 **لا يتحقق من توازن المدين/الدائن قبل إدراج قيد الإقفال** — قد يُنشئ قيد غير متوازن يُفسد كل التقارير المالية
  - **الإصلاح**: إضافة `isBalanced(totalDebit, totalCredit)` قبل الإدراج ← S11
- [ ] كشف قيد الإقفال السابق يستخدم مطابقة نصية: `.like("description", "%قيد إقفال السنة المالية%")` — هشة وقد تتطابق مع قيود أخرى
  - **الإصلاح**: استخدام حقل `reference_type = "fiscal_closing"` بدلاً من البحث في الوصف
- [ ] `posted_number` يُحسب بـ `MAX + 1` غير آمن ← S5

## أخطاء منطقية

- [ ] لا يتم التحقق من أن كل الفواتير والدفعات مرحّلة قبل الإقفال — قد يُقفل السنة مع مسودات معلّقة
- [ ] لا يتم إنشاء نسخة احتياطية قبل الإقفال
- [ ] لا يوجد إمكانية للتراجع عن الإقفال — عملية لا رجعة فيها
- [ ] أكواد الحسابات مكتوبة يدوياً ← S3
- [ ] منطق إنشاء القيد مكرر مع JournalEntryForm ← S4

## مشاكل تجربة المستخدم

- [ ] لا يوجد ملخص تفصيلي قبل التأكيد يوضح: الإيرادات، المصروفات، صافي الربح/الخسارة، بنود قيد الإقفال
- [ ] لا يوجد تأكيد متعدد الخطوات (مثل إدخال كلمة "إقفال" للتأكيد)
- [ ] لا يوجد مؤشر تقدم — العملية قد تأخذ وقتاً طويلاً
- [ ] لا يوجد سجل تاريخي لعمليات الإقفال السابقة

## صيانة وإعادة هيكلة

- [ ] استخراج منطق إنشاء القيد ← S4
- [ ] استخراج منطق `posted_number` ← S5
- [ ] استخراج كشف قيود الإقفال لدالة مشتركة مع TrialBalance, BalanceSheet, IncomeStatement

## اتساق

- [ ] يجب أن يستخدم نمط القيد المحاسبي الموحد ← S4
- [ ] يجب أن يستخدم `posted_number` الآمن ← S5

## التبعيات

- يعتمد على: `_shared-improvements.md` → S3, S4, S5, S11
