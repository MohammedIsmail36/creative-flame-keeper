# InventoryTurnoverReport.tsx — خطة التحسين

**الملف**: `src/pages/reports/InventoryTurnoverReport.tsx`  
**الأولوية**: 🟢 Low

---

## أخطاء منطقية

- [ ] **كشف "منتج جديد" معيب**: يستخدم `createdAt < 30 days` لكن `created_at` ليس دقيقاً (بيانات بذرية) — يجب الاعتماد على أول حركة شراء من `inventory_movements`
- [ ] **تصنيف معدل الدوران لا يراعي المخزون الآمن**: منتج بمعدل دوران منخفض عمداً (مواد خطرة مثلاً) يُصنّف خطأً
- [ ] **تصنيف ABC يستخدم الإيرادات بدلاً من الهامش**: منتج عالي الإيرادات منخفض الهامش يُصنّف "A" خطأً

## مشاكل تجربة المستخدم

- [ ] **واجهة مكتظة**: 6 أنواع فلاتر + نقاط أولوية + مصفوفة قرارات — كثير جداً في صفحة واحدة
- [ ] **إيموجي المصفوفة** (✅ ⚠️ 🔴 ❌) غير مهني وغير متاح لقارئات الشاشة — استبدال بأيقونات أو badges نصية
- [ ] لا يوجد drill-down من المصفوفة

## صيانة وإعادة هيكلة

- [ ] **تعقيد هائل**: 400+ سطر من منطق التجميع عرضة للانحدار — إضافة unit tests شاملة
- [ ] **سلسلة تحويلات بيانات هشة**: `salesByProduct` → `productMetrics` → `turnoverData` → `allTurnover` — استخراج لملف خدمة منفصل

## اتساق

- [ ] كشف المنتج الجديد يجب أن يتوافق مع ProductAnalytics (كلاهما يستخدم `NEW_PRODUCT_THRESHOLD_DAYS`)

## التبعيات

- يعتمد على: `_shared-improvements.md` → S8
- مرتبط بـ: ProductAnalytics (توحيد كشف المنتج الجديد)
