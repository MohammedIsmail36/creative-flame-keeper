# Dashboard.tsx — خطة التحسين

**الملف**: `src/pages/reports/Dashboard.tsx`  
**الأولوية**: 🟢 Low

---

## أخطاء منطقية

- [ ] **Race condition مع التحميل غير المتزامن**: عمليات متزامنة متعددة (`fetchKPIs`, `fetchSecondaryKPIs`, إلخ) بدون abort signals — تحديث state على مكوّن غير مُركّب عند التنقل
- [ ] **حساب COGS غير متسق**: يطرح تكاليف `sale_return` بشكل خاطئ — يجب عدّ COGS الفعلية فقط
- [ ] **حساب المنتجات الراكدة**: يستخدم 30 يوم كقيمة ثابتة غير قابلة للتخصيص
- [ ] **عدم التحقق من بيانات null متداخلة**: الوصول لـ `inv.customer?.name` بدون حراسة كاملة

## مشاكل تجربة المستخدم

- [ ] **لا توجد حالات فارغة للرسوم البيانية**: إذا لا توجد بيانات تظهر رسوم فارغة بدون توضيح
- [ ] **تحميل مجزّأ**: أعلام تحميل متعددة (`loadingKPIs`, `loadingSecondary`, إلخ) تعرض UI جزئياً — استخدام skeleton موحد
- [ ] **لا يوجد حدود خطأ (error boundaries)**: أخطاء الشبكة تعرض KPIs بقيمة صفر — المستخدم يظن البيانات صحيحة
- [ ] **30+ useState**: يجب استخدام `useReducer` أو context

## صيانة وإعادة هيكلة

- [ ] **تضخم الحالة**: 30+ استدعاء useState — استخراج لـ `useReducer` أو custom hook
- [ ] **أرقام سحرية**: "30 يوم" (سطر ~351)، "1.5 شهر" (سطر ~187) — استخراج لثوابت
- [ ] **تكرار نمط `Promise.all`** — استخراج لـ custom hook
- [ ] **دوال سهمية في JSX**: `onClick={() => navigate(to)}` — يجب تخزينها بـ useMemo/useCallback
- [ ] تنسيق التاريخ يستخدم `.slice()` بدلاً من date-fns ← S7

## اتساق

- [ ] تنسيق التاريخ مختلف عن باقي التقارير ← S7
- [ ] بنية بطاقات KPI مختلفة بين الأساسية والثانوية (grid مختلف، ظلال مختلفة)
- [ ] تسمية غير متسقة: `totalSales` vs `netSales` — خلط إجمالي وصافي

## التبعيات

- يعتمد على: `_shared-improvements.md` → S7, S8, S9
