# GrowthAnalytics.tsx — خطة التحسين

**الملف**: `src/pages/reports/GrowthAnalytics.tsx`  
**الأولوية**: 🟢 Low

---

## أخطاء منطقية

- [ ] **أخطاء حدود نطاق التاريخ**: حساب `prevFromDate`/`prevToDate` يستخدم طرح يدوي — خطأ مع حدود الأشهر (فبراير→مارس). يجب استخدام `subMonths` من date-fns
- [ ] **لا يعالج تقاطع حدود السنة المالية**: إذا كانت الشركة تستخدم سنة غير تقويمية، فترات المقارنة قد تكون خاطئة
- [ ] **حساب COGS قد يعدّ مرتين**: حركات `sale` و `sale_return` تحتاج معالجة دقيقة لتجنب أخطاء الاستبعاد

## مشاكل تجربة المستخدم

- [ ] **لا يوجد virtualization لجداول كبيرة**: آلاف الصفوف تسبب بطء
- [ ] **منتقي التاريخ لا يتحقق من النطاق**: يمكن اختيار تاريخ نهاية قبل تاريخ البداية
- [ ] لا يوجد رسم بياني للمقارنة بين الفترات

## صيانة وإعادة هيكلة

- [ ] **تعقيد عالي**: 600+ سطر — يجب تقسيم لمكوّنات فرعية لكل نوع KPI
- [ ] نمط الاستعلام مكرر (select, filter by date, sum) — استخراج لـ query builder

## اتساق

- [ ] توحيد تنسيق التواريخ ← S7
- [ ] توحيد نمط التصدير مع باقي التقارير

## التبعيات

- يعتمد على: `_shared-improvements.md` → S7, S8
