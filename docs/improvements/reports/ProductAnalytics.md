# ProductAnalytics.tsx — خطة التحسين

**الملف**: `src/pages/reports/ProductAnalytics.tsx`  
**الأولوية**: 🟢 Low

---

## أخطاء منطقية

- [ ] **تصنيف ABC لا يعالج حالة الإيرادات الصفرية**: إذا كانت كل المنتجات بإيرادات = 0، الحساب التراكمي يفشل بقسمة على صفر
  - **الإصلاح**: إضافة حراسة `if (totalRevenue === 0) return all as C`
- [ ] **تصنيف معدل الدوران يستخدم عتبات أيام ثابتة**: الأيام المثلى تختلف بنوع المنتج (FMCG vs سلع فاخرة)
- [ ] **حساب نسبة المرتجعات عرضة للقيم المتطرفة**: منتج بـ 10 مبيعات و8 مرتجعات = 80% — لا دلالة إحصائية بحجم عينة صغير
  - **الإصلاح**: إضافة حد أدنى لحجم العينة (مثلاً >= 20 مبيعة) أو إظهار تحذير

## مشاكل تجربة المستخدم

- [ ] **6 أنواع عرض في صفحة واحدة**: top-sellers, profitable, category, turnover, ABC, returns — كثير جداً
- [ ] فلتر الصنف لا يُعاد تعيينه عند تغيير العرض — يعني أشياء مختلفة في كل سياق
- [ ] الرسم البياني يعرض فقط أعلى 10 — باقي البيانات غير متاحة بدون تصدير

## صيانة وإعادة هيكلة

- [ ] **مكوّن ضخم** (600+ سطر) — استخراج مكوّن فرعي لكل نوع عرض
- [ ] **حمل استعلامات كبير**: 6 استعلامات منفصلة — دمج في 2-3 backend views
- [ ] خلط مستويات تعقيد (ترتيب بسيط + مصفوفة ABC معقدة) في ملف واحد

## اتساق

- [ ] كشف المنتج الجديد يجب أن يتوافق مع InventoryTurnoverReport

## التبعيات

- مرتبط بـ: InventoryTurnoverReport
