# SalesReport.tsx — خطة التحسين

**الملف**: `src/pages/reports/SalesReport.tsx`  
**الأولوية**: 🟢 Low

---

## أخطاء منطقية

- [ ] **لا يعالج المنتجات المحذوفة**: إذا حُذف منتج بعد إنشاء الفاتورة، `product` relation يكون null — خطأ وقت التشغيل
  - **الإصلاح**: إضافة fallback: `product?.name ?? "منتج محذوف"`
- [ ] **متغير `isPostedOnly` مُعرّف لكن لا يُستخدم في الفلترة**: يجب تطبيقه على حسابات KPIs
- [ ] **نسبة التحصيل محددة بسقف 100** بدون توضيح — قد يخفي مشاكل في جودة البيانات

## مشاكل تجربة المستخدم

- [ ] لا توجد حالة فارغة عند عدم وجود بيانات تطابق الفلاتر
- [ ] لا يوجد مؤشر تقدم للتصدير الكبير
- [ ] مؤشر التأخر يظهر فقط للفواتير المرحّلة — لا يحذّر من المسودات التي ستتأخر

## صيانة وإعادة هيكلة

- [ ] نمط التصدير مكرر مع باقي التقارير — استخراج لمكوّن/hook مشترك
- [ ] نمط الفلترة وإعادة التحميل مكرر

## اتساق

- [ ] توحيد نمط التصدير مع باقي التقارير
- [ ] توحيد نمط معالجة الأخطاء ← S8

## التبعيات

- يعتمد على: `_shared-improvements.md` → S8
