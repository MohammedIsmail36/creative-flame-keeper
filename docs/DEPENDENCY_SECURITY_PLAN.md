# خطة معالجة ثغرات اعتماديات الواجهة

> تاريخ التحليل: 2026-09-06  
> الحالة: S3-3A وS3-3B مغلقتان ومعتمدتان على Staging وFarida وAlibea؛ لا توجد دفعة نشر معلقة
> البيئة المرجعية: Node `v22.23.2` وnpm `10.9.8`

## الهدف والضوابط

الهدف خفض مخاطر الاعتماديات تدريجياً دون تعريض الإنتاج لتحديثات كبرى غير مختبرة. قواعد التنفيذ:

- لا يُستخدم `npm audit fix --force`.
- كل دفعة مستقلة، وتنجح أولاً في TypeScript وجميع الاختبارات والبناء.
- تُنشر الدفعة على Staging وتُختبر وظيفياً قبل Farida، ثم تعتمد Farida قبل Alibea.
- لا تمس أي دفعة قواعد البيانات أو migrations أو Edge Functions ما لم تُذكر صراحة.
- تظل الأبنية الحالية على Farida وAlibea دون تغيير أثناء التحليل.

## خط الأساس

أظهر `npm audit` في 2026-09-06:

| النطاق | حرجة | عالية | متوسطة | الإجمالي |
|---|---:|---:|---:|---:|
| جميع الاعتماديات | 0 | 4 | 10 | 14 |
| اعتماديات الإنتاج عبر `--omit=dev` | 0 | 2 | 6 | 8 |

خروج الأمر بالحالة `1` متوقع لأن التقرير يحتوي نتائج، ولا يعني فشل npm أو تلف lockfile.

الإنتاج واجهة Vite مبنية كملفات ثابتة يقدمها Nginx؛ لا يعمل خادم Vite أو Drizzle في مسار طلبات الإنتاج. لذلك تنبيهات خادم التطوير لا تعني أن Nginx أو قواعد البيانات مكشوفة بها، لكنها تظل ديوناً يجب معالجتها داخل بيئة البناء.

## نتيجة المحاكاة الآمنة

نُسخ `package.json` و`package-lock.json` إلى `/tmp/accounting-audit-simulation-20260906`، وشُغّل `npm audit fix --package-lock-only --ignore-scripts` هناك فقط. لم يُعدل المستودع، ثم حُذف المجلد المؤقت.

غيرت المحاكاة 14 مدخلاً فقط في lockfile، وأهم الترقيات:

| الحزمة | قبل | بعد |
|---|---:|---:|
| `brace-expansion` | `1.1.16` / `2.1.2` / `5.0.8` | `1.1.18` / `2.1.4` / `5.0.9` |
| `dompurify` | `3.4.12` | `3.4.15` |
| `nanoid` | `3.3.16` | `3.3.18` |
| `js-yaml` | `4.3.0` | `4.3.2` |
| `yaml` | `2.6.0` | `2.9.0` |
| `react-router` و`react-router-dom` | `6.30.4` | `6.30.6` |
| `@remix-run/router` | `1.23.3` | `1.23.4` |

بعد المحاكاة انخفض التقرير الكامل من 14 إلى 9، وانخفض تقرير اعتماديات الإنتاج من 8 إلى 4 متوسطة فقط، وأصبحت النتائج العالية والحرجة في مسار الإنتاج صفراً. لم تُشغل اختبارات المشروع على lockfile المحاكى لأن هذه كانت خطوة قياس فقط وليست اعتماداً للتحديث.

## تقييم النتائج المتبقية

### 1. React Router — خطر قابل للوصول ويحتاج أولوية

- الإصدار الحالي `6.30.4`، والمحاكاة ترفعه داخل النطاق إلى `6.30.6`، لكن تنبيهين في `react-router` يبقيان حتى الانتقال إلى `7.18.x`.
- التطبيق SPA ولا يستخدم SSR أو hydration خادمية، لذلك تنبيه constructor injection الخاص بـSSR غير قابل للمسار الحالي.
- يوجد مسار فعلي يحتاج تحصيناً: `src/pages/Auth.tsx` يأخذ `next` من query string، ويقبل حالياً أي قيمة تبدأ `/` ولا تبدأ `//`. القيمة التي تحتوي backslash مثل `/\evil.example` قد تتوافق مع سيناريو open redirect المذكور في التنبيه.
- العلاج الفوري ليس انتظار الترقية الكبرى: إضافة دالة مركزية تسمح بمسار داخلي same-origin فقط، وترفض backslash والبروتوكولات والمسارات المزدوجة والقيم غير الصالحة، مع اختبارات وحدات.

المراجع: `GHSA-wrjc-x8rr-h8h6` و`GHSA-337j-9hxr-rhxg` و`GHSA-jjmj-jmhj-qwj2`.

### 2. ExcelJS / UUID — نتيجة متوسطة غير مستغلة بالطريقة الحالية

- `exceljs@4.4.0` هو أحدث إصدار، ويعتمد `uuid@^8.3.0`؛ لا يوجد إصدار ExcelJS أحدث يصلح السلسلة حالياً.
- اقتراح npm بتخفيض ExcelJS إلى `3.4.0` غير مناسب ولا يُعتمد، لأنه تغيير رجوعي كبير ولا يمثل ترقية آمنة.
- تنبيه UUID يخص v3/v5/v6 عند تمرير Buffer. فحص ExcelJS المثبت وجد استدعاء `uuid.v4()` فقط ومن دون Buffer، ولم يجد المشروع استدعاءً مباشراً لـUUID؛ لذا لا يطابق الاستخدام الحالي مسار الاستغلال المعلن.
- يظل التنبيه مسجلاً حتى إصدار ExcelJS متوافق أو نجاح اختبار override إلى `uuid>=11.1.1` في بيئة معزولة، خصوصاً لاختبارات قراءة/كتابة وتصدير Excel.

المرجع: `GHSA-w5hq-g745-h8pq`.

### 3. Vite / esbuild — أدوات بناء وليست خدمة إنتاج

- `vite@5.4.21` يحتاج ترقية كبرى إلى `8.2.2` وفق التقرير، ومعه `@vitejs/plugin-react-swc` من 3 إلى 4 وفحص توافق Vitest.
- التنبيهات تخص خادم التطوير وقراءة ملفات أو طلبات من مواقع أخرى، وبعضها خاص بمسارات Windows. خادم Vite غير منشور؛ Nginx يقدم ملفات `dist` الثابتة.
- لا تُنفذ الترقية الكبرى مع الدفعة الآمنة؛ تُختبر مستقلة على نسخة نظيفة لأن أثرها يقع على البناء والاختبارات.

المراجع: `GHSA-4w7w-66w2-5vf9` و`GHSA-v6wh-96g9-6wx3` و`GHSA-fx2h-pf6j-xcff` و`GHSA-67mh-4wv8-2f99`.

### 4. Drizzle Kit / esbuild-kit — تطوير فقط

- `drizzle-kit@0.31.10` هو أحدث إصدار وما زال يعتمد `@esbuild-kit/esm-loader` الذي يجلب `esbuild@0.18.20`.
- الاستخدام المحلي محصور في `drizzle.config.ts`، ولا يوجد script لتشغيل Drizzle ضمن `package.json` ولا يدخل خادم الإنتاج.
- اقتراح npm بتخفيض Drizzle Kit إلى `0.18.1` مرفوض لأنه تغيير رجوعي كبير. يُراجع لاحقاً إمكان إزالة الأداة إن كانت غير مستخدمة فعلياً أو انتظار إصلاح upstream.

### 5. الإصلاحات داخل النطاق الحالي

- `brace-expansion`: DoS عبر توسع غير محدود؛ تحلها الإصدارات المرقعة في المحاكاة.
- `dompurify`: XSS؛ يأتي اختيارياً عبر jsPDF ويُرفع إلى `3.4.15` داخل النطاق الحالي.
- `nanoid`: loop عند generator مخصص بحجم صفر؛ يأتي عبر PostCSS ويُرفع إلى `3.3.18`.
- `js-yaml` و`yaml`: استهلاك CPU أو stack overflow عند YAML خبيث؛ استخدامهما هنا في أدوات lint/config، وتوجد ترقيات متوافقة.

## خطة التنفيذ المرحلية

| المرحلة | العمل | بوابة الاعتماد |
|---|---|---|
| S3-3A | معتمدة على Staging ومنشورة على Farida بانتظار الاختبار: تحصين `next` والمسارات الداخلية بدالة مركزية واختبارات للقيم العادية و`//` وbackslash والقيم المشفرة والبروتوكولات | نجح lint وTypeScript و48 ملفاً/563 اختباراً والبناء، ثم اختبارات Staging؛ نُشرت على Farida فقط بنسخة رجوع وحواجز API |
| S3-3B | تطبيق تحديث lockfile المتوافق المحاكى، دون `--force` أو تغيير النطاقات المباشرة | تثبيت نظيف، 540 اختباراً على الأقل، بناء، `npm audit` كامل وإنتاجي، ثم Staging |
| S3-3C | تجربة ترقية Vite 8 وplugin المتوافق وأي تحديث Vitest لازم داخل نسخة معزولة | لا اعتماد قبل نجاح البناء والاختبارات ومراجعة config وحجم/تقسيم الأصول |
| S3-3D | ترقية React Router 7 كتغيير مستقل بعد بقاء تحصين التطبيق | مراجعة جميع routes والتنقل والحماية وMFA والدخول، ثم دورة Staging كاملة |
| S3-3E | اختبار override لـUUID أو انتظار إصدار ExcelJS؛ يمنع خفض ExcelJS | نجاح اختبارات Excel roundtrip وتصدير تقارير حقيقية على Staging وعدم تغير الملفات الناتجة وظيفياً |
| S3-3F | تقرير استخدام Drizzle واتخاذ قرار الإزالة أو الانتظار/الترقية | إثبات عدم الحاجة أو نجاح أوامر schema المعتمدة؛ لا إزالة بالتخمين |

## تنفيذ S3-3A — 2026-09-06

- أضيفت `src/lib/safe-internal-path.ts` بدالة `getSafeInternalPath()` التي لا تقبل إلا مساراً يبدأ `/` ويبقى داخل نفس الأصل بعد تحليل URL.
- ترفض الدالة البروتوكولات والمسارات التي تبدأ `//` وbackslash ومحارف التحكم، كما ترفض slash وbackslash المشفرين مع طبقات `%25` المتكررة لمنع تجاوز الفحص بالترميز المزدوج أو الأعمق.
- استبدل الفحص اليدوي في `src/pages/Auth.tsx` بالدالة المركزية قبل تمرير `next` إلى `<Navigate>` أو `navigate()`، وحُسنت معاملة خطأ الدخول من `any` إلى `unknown` بلا تغيير لمسار النجاح.
- أضيفت 23 حالة اختبار تقبل المسارات الداخلية والاستعلام والـhash والتطبيع، وترفض الرابط المطلق والبروتوكولي والمسار المزدوج وbackslash المباشر والمشفر والترميز المتكرر والقيمة التالفة ومحارف التحكم المباشرة ومتعددة الترميز.
- نجح ESLint للملفات الثلاثة المعدلة، ونجح TypeScript، ونجح 48 ملف اختبار/563 اختباراً، ونجح بناء Vite بعد تحويل `3936` وحدة خلال `44.00s`.
- لم تتغير `package.json` أو `package-lock.json`، ولم تُحدث أي حزمة، ولم تُطبق migration أو Edge Function، ولم يُنشر البناء المحلي.
- رُفعت S3-3A بالالتزام `1465940`، ثم بُنيت بمتغيرات Staging الصريحة والمفتاح العام `anon` الذي بقي في ذاكرة العملية ولم يُطبع أو يُحفظ.
- اجتاز البناء حواجز العزل: احتوى عنوان المشروع `dunzfxurefzlaamgghys` وكود التحصين، ولم يحتو عناوين Farida أو Alibea أو مشروع Lovable القديم، وكانت مسارات الأصول من جذر نطاق Staging.
- قبل النشر حُفظت واجهة Staging السابقة في `/opt/backups/accounting-app/staging-s3-3a-before-20260906-143336`؛ تضم 183 ملفاً، وتطابقت بصمة `index.html` المرجعية `5beab284702adc7e538a3333a265604760c079f0a1268697ee6f20cfd89262c3`.
- نُسخ البناء إلى `/var/www/staging.alibea2020.com` فقط. يخدم النطاق الأصل `/assets/index-DuzwPCux.js` ببصمة `e953a48bf3abd1d7c2876ce3ffc7ba273db835252740a7660cc2e2e0cd679c08`، وبصمة `index.html` هي `de5ac1773ee9459a6cb04b76abeba37d5e7af3df05caa349453dd068edc96759`.
- تطابقت استجابات `/` و`/auth` والأصل الرئيسي عبر HTTPS مع الملفات المنشورة، وأعادت Staging وFarida وAlibea HTTP 200. ظلت بصمات أصول Farida وAlibea الأربع مطابقة لما قبل النشر، وبقي Nginx وDocker نشطين.
- حُذف مجلد البناء المؤقت بعد نجاح التحقق. لم تُنشر S3-3A على Farida أو Alibea، ولم تتغير قاعدة Staging أو قواعد الإنتاج.
- نجح اختبار المستخدم للمسار الداخلي الآمن `next=/reports/sales`: انتقل حساب المدير إلى تقرير المبيعات داخل نطاق Staging، بينما لم يسمح حساب البائع بعبور صلاحيات التقرير. هذه هي النتيجة الصحيحة وتثبت أن تحصين `next` لا يتجاوز حماية الأدوار.
- نجح اختبار المستخدم للمسار الخبيث `next=/\evil.example`: رفضت الدالة القيمة وأعادت المستخدم إلى لوحة التحكم داخل `staging.alibea2020.com`، ولم يفتح المتصفح النطاق الخارجي. بذلك اعتُمدت S3-3A وظيفياً على Staging.
- قبل Farida حُفظت نسخة رجوع كاملة في `/opt/backups/accounting-app/farida-s3-3a-before-20260906-150422` تضم 183 ملفاً بصلاحية `0700`. طابقت المصدر مباشرة؛ بصمة الصفحة السابقة `9782c74b3ab5cb61250b12b4ebb630c1ee3724a475e63c627aed19ba2d4915dd` وبصمة الأصل السابق `091b483b6450e8ec20c42925fa48fef8b3ef290d`.
- ملاحظة تشغيلية: توقف اختبار النسخة الأول قبل النشر لأن القيمة المرجعية المكتوبة يدوياً للأصل كانت بصمة مختصرة من 40 حرفاً؛ لم يبدأ النشر. اعتُمدت النسخة بالمقارنة الثنائية الكاملة وإخراج SHA-256 ذي 64 حرفاً، ثم أعيدت صلاحية جذر النسخة إلى `0700` بعد أن ورثها `rsync -a` من المصدر.
- شُغل `scripts/deploy-all.sh --only farida --no-pull --skip-db --skip-functions` مع `VITE_APP_ENV=production` ومسار `/`. نفذ `npm ci` من lockfile وأظهر خط الأساس نفسه 14 نتيجة من دون تشغيل `audit fix`، ثم بنى `3936` وحدة خلال `45.06s` ونشر Farida وحدها.
- يخدم Farida الآن `/assets/index-DvD8IDVh.js` ببصمة `073b8a836942d57101ce8e05ad5078af4205e26bfc0d65316e5da47cc413fa3c`، وبصمة `index.html` هي `39b42463904ace0ab2f1f4c8cffb1420de85523d60ad58b8bfccfb01b9af7c8b`. تطابقت `/` و`/auth` والأصل الحي مع ملفات النشر، واحتوت الحزمة API الخاص بـFarida والتحصين ولم تحتو عناوين Alibea أو Staging أو Lovable.
- ملاحظة تحقق: توقف الفحص الحي الأول عند مقارنة بصمة `index.html` يدوية قديمة لـAlibea؛ لم يتغير أي ملف. أعيد الفحص من ملف البصمات المرجعي الرسمي فاجتازت أصول Alibea، كما بقيت بصمات Staging مطابقة. أعادت النطاقات الثلاثة HTTP 200 وبقي Nginx وDocker نشطين.
- لم تُطبق migration أو Edge Function، ولم تتغير `package.json` أو `package-lock.json`، ولم تُنشر S3-3A على Alibea.
- نجح اختبار المستخدم للمسار الداخلي `next=/reports/sales` بحساب المدير على Farida، وانتقل إلى تقرير المبيعات داخل نطاق Farida كما هو متوقع.
- نجح اختبار المستخدم للمسار الخبيث `next=/\\evil.example` على Farida: رُفض التحويل وبقي المستخدم داخل `farida.alibea2020.com`. بذلك اعتُمدت S3-3A وظيفياً على Farida.
- قبل نشر Alibea أُنشئت نسخة الرجوع `/opt/backups/accounting-app/alibea-s3-3a-before-20260906-151305` بصلاحية `0700`. تضم 183 ملفاً، واجتازت المقارنة الثنائية الكاملة مع `/var/www/alibea`، وتطابقت بصمة `index.html` للطرفين: `fb69bfe38f167feb85b03995b0160aafd17954560962e7ff9e4c648a7cf9cb64`. لم يُبنَ أو يُنشر شيء في هذه الخطوة.
- توقفت محاولة البناء المؤقتة الأولى قبل تشغيل Vite بسبب خطأ داخلي من npm (`Exit handler never called`) عند تعذر كتابة سجل npm في بيئة العزل؛ لم تمس ملفات التطبيق أو الإنتاج. أُعيدت المحاولة في `/tmp/accounting-alibea-s3-3a-build-retry-20260906-151305` باستخدام `node_modules` المثبتة في المشروع بعد التحقق من بصمة lockfile.
- نجح بناء Alibea المعزول بإعدادات الإنتاج الصريحة بعد تحويل `3936` وحدة خلال `51.33s`. يتكون من 183 ملفاً، وأصله الرئيسي `/assets/index-JGMdg96S.js`. بصمة `index.html` هي `dbcaa66e7888bacd60be7f459924e628984148eafc3dfc07244e4ff93aa8a116` وبصمة الأصل الرئيسي `292929623f39ef66befb9d36c663f46c72ce6f21d220ccfd65a5fc2760f1a6a2`.
- اجتاز البناء حواجز العزل: احتوى API الخاص بـAlibea ولم يحتو API الخاص بـFarida أو مشروع Staging أو مشروع Lovable القديم. بقيت ملفات Alibea الحية مطابقة لنسخة الرجوع، وأعادت النطاقات الثلاثة HTTP 200، وبقي Nginx وDocker نشطين. لم تتغير `package.json` أو `package-lock.json`، ولم يُنشر البناء بعد.
- نُسخ البناء المتحقق منه إلى `/var/www/alibea` وحده باستخدام تحديث مؤجل وحذف الأصول القديمة ذات البصمات. تطابقت الملفات الحية ثنائياً مع البناء (183 ملفاً)، وطابقت استجابة HTTPS لكل من الصفحة والأصل الرئيسي بصمتي البناء. أعادت `/auth` على Alibea وواجهتا Farida وStaging HTTP 200، وبقي Nginx وDocker نشطين.
- ظلت بصمات Farida (`index.html` و`/assets/index-DvD8IDVh.js`) وStaging (`index.html` و`/assets/index-DuzwPCux.js`) مطابقة لقيمها المعتمدة. لم تُطبق migration أو Edge Function، ولم تتغير أي قاعدة بيانات.
- نجح اختبار المستخدم للمسار الداخلي `next=/reports/sales` بحساب المدير على Alibea، وانتقل إلى تقرير المبيعات داخل نطاق Alibea كما هو متوقع.
- نجح اختبار المستخدم للمسار الخبيث `next=/\\evil.example` على Alibea: رُفض التحويل وبقي المستخدم داخل `alibea.alibea2020.com`. بذلك اعتُمدت S3-3A وظيفياً على Staging وFarida وAlibea وأُغلق نشرها.

## الخطوة التالية

رفع توثيق تنظيف S3-3B إلى Git، ثم إجراء مراجعة تخطيطية للقراءة فقط لـS3-3C الخاصة بترقية Vite الكبرى وتوافق Plugin React وVitest وNode. لا تُحدّث حزمة ولا يبدأ تنفيذ أو نشر دون خطة مستقلة وموافقة المستخدم.

## بدء S3-3B — 2026-09-06

- أُعيد قياس خط الأساس من سجل npm مباشرة على Node `v22.23.2` وnpm `10.9.8`: التقرير الكامل 14 نتيجة (4 عالية و10 متوسطة)، وتقرير `--omit=dev` ثماني نتائج (2 عالية و6 متوسطة)، بلا نتائج حرجة.
- فشل طلب أول داخل العزل بسبب DNS وتعذر كتابة سجل npm؛ رُفض ناتجه الفارغ ولم يُستخدم في التقييم. نجحت الإعادة بذاكرة npm مؤقتة واتصال قراءة بالسجل، وطابقت خط الأساس الموثق سابقاً.
- بقيت بصمتا `package.json` و`package-lock.json` كما هما: `529d244013ec82e9f47cad1586efaf46921e0594206fbb53a1dc66f03f640c91` و`266f68cb4564d0acefea86bc0803572f5aae741a7719eab510885e1c926ee786`. مساحة العمل نظيفة وقت القياس، ولم يُشغل `audit fix` ولم يُثبت أو يُنشر شيء.
- أُنشئت محاكاة lockfile جديدة في `/tmp/s3-3b-lock-simulation-20260906-153000` وشُغل فيها فقط `npm audit fix --package-lock-only --ignore-scripts` من دون `--force`. أعاد npm الحالة `1` بعد إتمام التحديث لأن تسع نتائج متبقية تتطلب تغييرات كبرى؛ عوملت هذه الحالة كما ينبغي ولم يُعد تنفيذ الإصلاح.
- لم يتغير `package.json`. تغيرت 14 خانة حزمة داخل lockfile: ترقيات `@remix-run/router` إلى `1.23.4` و`brace-expansion` إلى `1.1.18`/`2.1.4`/`5.0.9` و`dompurify` إلى `3.4.15` و`js-yaml` إلى `4.3.2` و`nanoid` إلى `3.3.18` و`react-router`/`react-router-dom` إلى `6.30.6` و`yaml` إلى `2.9.0`، مع نقل مواضع `balanced-match` و`brace-expansion` المتداخلة وفق حل npm. لم يحدث تغيير مباشر كبير.
- أصبحت بصمة lockfile المحاكى `cca7687a37b83e70be963113d4fdb5436b86f5bc8952287a9871bb89d6526d5c`، وفارق النص 58 إضافة و33 حذفاً. انخفض التقرير الكامل إلى 9 (1 عالية و8 متوسطة)، والإنتاجي إلى 4 متوسطة فقط مع صفر عالية أو حرجة.
- النتائج الإنتاجية المتبقية محصورة في React Router وExcelJS/UUID وتتطلب ترقيات كبرى مؤجلة إلى S3-3D وS3-3E. النتيجة العالية الوحيدة المتبقية في التقرير الكامل تخص Vite أداة البناء المؤجلة إلى S3-3C. بقي lockfile المستودع ببصمته الأصلية ولم تُنقل المحاكاة أو تُثبت أو تُنشر.
- أُنشئت نسخة تحقق مستقلة في `/tmp/s3-3b-verification-20260906-154000` واستخدمت lockfile المحاكى فقط. نجح `npm ci` وثبت 652 حزمة، ثم نجح TypeScript بلا أخطاء.
- نجحت مجموعة الاختبارات كاملة: 48 ملف اختبار و563 اختباراً خلال `56.54s`، بما فيها اختبارات التقارير والمحاسبة والمخزون وExcel والتحويل الداخلي الآمن.
- نجح بناء Vite للإنتاج بعد تحويل `3936` وحدة خلال `45.26s` وأنتج 183 ملفاً. بقي تحذير تقسيم `expense-posting` وتحذير الأجزاء الأكبر من 800KB كما في البناء السابق؛ لم يكونا فشلاً ولم ينتج عنهما تغير وظيفي.
- تأكد بعد البوابة أن `package.json` وlockfile المستودع ما زالا ببصمتيهما الأصليتين، وأن التعديل بقي محصوراً في `/tmp`. لم يُنقل lockfile ولم يُبنَ ببيانات شركة ولم يُنشر شيء إلى Staging أو الإنتاج.
- نُقل lockfile المتحقق منه وحده إلى المستودع برقعة محددة. طابق بالبايت نسخة المحاكاة والاختبار، وبقيت بصمته `cca7687a37b83e70be963113d4fdb5436b86f5bc8952287a9871bb89d6526d5c` وفرقُه 58 إضافة و33 حذفاً. بقي `package.json` دون تغيير، ولم تُثبت حزم داخل المشروع ولم يُنشأ التزام أو يُنشر شيء.
- ثُبتت S3-3B في الالتزام `f4fb9a5` ورُفعت إلى `origin/main`، مع تطابق الفرعين ونظافة مساحة العمل. لم يحدث نشر في خطوة الالتزام.
- بُنيت واجهة Staging من الالتزام النهائي داخل `/tmp/s3-3b-staging-build-20260906-160000`، باستخدام الحزم التي ثبتها `npm ci` من lockfile الجديد ومفتاح Staging العام داخل ذاكرة العملية فقط. نجح Vite بعد تحويل `3936` وحدة خلال `43.22s` وأنتج 183 ملفاً.
- الأصل الرئيسي الجاهز هو `/assets/index-DeU0Y8Mc.js` ببصمة `eee96a4a927d10fb20477fea8731bf0b5c5725f5bffb95ad0bd79b4846ca940f`، وبصمة `index.html` هي `f86be6682086cb363d0de838f158d8a3c43859b123f3b86a28f178cdd0465440`.
- اجتاز البناء حواجز Staging: احتوى عنوان المشروع `dunzfxurefzlaamgghys` وعلامة البيئة التجريبية ومسارات `/assets/` من جذر النطاق، ولم يحتو API الخاص بـFarida أو Alibea ولا مشروع Lovable القديم. بقيت بصمات البيئات الثلاث المنشورة مطابقة، وأعادت HTTP 200، وبقي Nginx وDocker نشطين. لم يُنشر البناء بعد.
- قبل النشر حُفظت واجهة Staging الحالية في `/opt/backups/accounting-app/staging-s3-3b-before-20260906-153857` بصلاحية `0700`. تضم النسخة 183 ملفاً، واجتازت المقارنة الثنائية الكاملة، وتطابقت بصمة `index.html` للطرفين: `de5ac1773ee9459a6cb04b76abeba37d5e7af3df05caa349453dd068edc96759`. لم يتغير Staging أو الإنتاج في خطوة النسخ الاحتياطي.
- نُشر البناء المتحقق منه على `/var/www/staging.alibea2020.com` وحده باستخدام تحديث مؤجل وحذف أصول Staging القديمة ذات البصمات. تطابقت الملفات الحية ثنائياً مع البناء (183 ملفاً)، وطابقت استجابات HTTPS للصفحة والأصل الرئيسي بصمتي `f86be6682086cb363d0de838f158d8a3c43859b123f3b86a28f178cdd0465440` و`eee96a4a927d10fb20477fea8731bf0b5c5725f5bffb95ad0bd79b4846ca940f`.
- أعادت الصفحة الرئيسية و`/auth` على Staging وواجهتا Farida وAlibea HTTP 200. بقي Nginx وDocker نشطين، وظلت بصمات Farida وAlibea المعتمدة مطابقة. أكد الفحص الحي وجود API الخاص بـStaging وعلامة البيئة وغياب عناوين الإنتاج ومشروع Lovable القديم. لم تتغير قواعد البيانات أو Edge Functions.
- نجح أول اختبار وظيفي للمستخدم بعد `Ctrl+F5`: حُمّلت Staging، ونجح تسجيل دخول المدير، وفتحت لوحة التحكم بصورة طبيعية مع ظهور علامة «بيئة تجريبية» في النافبار.
- نجح التنقل من القائمة إلى «التقارير ← تقرير المبيعات»، وفتحت الصفحة وعرضت بيانات Staging التجريبية دون صفحة بيضاء أو خطأ.
- نجح تبديل «فترة سريعة» من «هذا الشهر» إلى «هذا الربع»؛ تغير نطاق التاريخ وبيانات التقرير بصورة طبيعية في المرتين دون تعليق أو خطأ.
- نجح فتح قائمة التصدير وتنزيل تقرير PDF وفتح الملف، وظهر محتوى التقرير بصورة صحيحة دون خطأ.
- نجح تنزيل تقرير Excel وفتح الملف، وظهرت بيانات التقرير داخله بصورة سليمة. بذلك اجتاز مسارا PDF وExcel اختبار التصدير بعد تحديث lockfile.
- نجح اختبار `next=/reports/sales` بحساب المدير بعد تحديث React Router إلى `6.30.6`، وانتقل إلى تقرير المبيعات داخل نطاق Staging كما هو متوقع.
- نجح اختبار `next=/\\evil.example` بعد تحديث React Router: رُفض المسار وبقي المستخدم داخل `staging.alibea2020.com` وانتقل إلى لوحة التحكم دون فتح نطاق خارجي. بذلك اعتُمدت S3-3B وظيفياً على Staging بعد نجاح الدخول والتنقل والفترات وتصدير PDF وExcel ومساري التحويل.
- رُفع توثيق اعتماد Staging في الالتزام `5fdbcef` وتطابق `main` مع `origin/main` دون نشر إنتاجي.
- قبل تجهيز Farida حُفظت واجهتها الحالية في `/opt/backups/accounting-app/farida-s3-3b-before-20260906-155138` بصلاحية `0700`. تضم 183 ملفاً واجتازت المقارنة الثنائية الكاملة. تطابقت بصمة `index.html` للطرفين `39b42463904ace0ab2f1f4c8cffb1420de85523d60ad58b8bfccfb01b9af7c8b` وبصمة الأصل `/assets/index-DvD8IDVh.js` للطرفين `073b8a836942d57101ce8e05ad5078af4205e26bfc0d65316e5da47cc413fa3c`. لم يحدث بناء أو نشر.
- بُنيت Farida من الالتزام النهائي داخل `/tmp/s3-3b-farida-build-20260906-160000` باستخدام lockfile الجديد والحزم المثبتة منه ومتغيرات Farida الصريحة. نجح Vite بعد تحويل `3936` وحدة خلال `49.37s` وأنتج 183 ملفاً.
- الأصل الرئيسي الجاهز هو `/assets/index-JPiGv4kC.js` ببصمة `822e13108224e6bf738f853aa632aebeb41d94303c94e67495794aec212bf37d`، وبصمة `index.html` هي `81b00744793d06a2641ef6b2380dec5a9eb56e8ef29418b88173653fa4cda388`.
- اجتاز البناء حواجز Farida: احتوى API الخاص بها ومسارات `/assets/` من جذر النطاق، ولم يحتو API الخاص بـAlibea أو مشروع Staging أو مشروع Lovable القديم. بقيت Farida الحية مطابقة لنسخة الرجوع، وبقيت بصمات Alibea وStaging المعتمدة كما هي. أعادت النطاقات الثلاثة HTTP 200 وبقي Nginx وDocker نشطين. لم يُنشر البناء بعد.
- نُشر البناء المتحقق منه على `/var/www/farida` وحده باستخدام تحديث مؤجل وحذف أصول Farida القديمة ذات البصمات. تطابقت الملفات الحية ثنائياً مع البناء (183 ملفاً)، وطابقت استجابات HTTPS للصفحة والأصل الرئيسي بصمتي `81b00744793d06a2641ef6b2380dec5a9eb56e8ef29418b88173653fa4cda388` و`822e13108224e6bf738f853aa632aebeb41d94303c94e67495794aec212bf37d`.
- أكد الفحص الحي وجود API الخاص بـFarida وغياب عناوين Alibea وStaging وLovable. أعادت الصفحة الرئيسية و`/auth` على Farida وواجهتا Alibea وStaging HTTP 200، وبقي Nginx وDocker نشطين. ظلت بصمات Alibea وStaging المعتمدة مطابقة، ولم تتغير قاعدة بيانات أو Edge Function.
- نجح أول اختبار وظيفي على Farida بعد `Ctrl+F5`: حُمّلت الواجهة، ونجح تسجيل دخول المدير، وفتحت لوحة التحكم بصورة طبيعية.
- نجح التنقل إلى «التقارير ← تقرير المبيعات»، وفتحت الصفحة وعرضت بيانات Farida الصحيحة دون صفحة بيضاء أو خطأ.
- نجح تبديل الفترة السريعة بين «هذا الشهر» و«هذا الربع»؛ تغير نطاق التاريخ وبيانات Farida بصورة طبيعية دون تعليق أو خطأ.
- نجح تصدير PDF من تقرير Farida، ونُزل الملف وفُتح وظهر التقرير داخله بصورة صحيحة دون خطأ.
- نجح تصدير Excel من تقرير Farida، ونُزل الملف وفُتح وظهرت بيانات التقرير داخله بصورة سليمة.
- نجح اختبار `next=/reports/sales` بحساب المدير على Farida بعد تحديث React Router إلى `6.30.6`، وانتقل إلى تقرير المبيعات داخل نطاق Farida كما هو متوقع.
- نجح اختبار `next=/\\evil.example` على Farida: رُفض المسار وبقي المستخدم داخل `farida.alibea2020.com` وانتقل إلى لوحة التحكم دون فتح نطاق خارجي. بذلك اعتُمدت S3-3B وظيفياً على Farida بعد نجاح الدخول والتقرير والفترات وتصدير PDF وExcel ومساري التحويل.
- رُفع توثيق اعتماد Farida في الالتزام `9fc78ac` وتطابق `main` مع `origin/main` دون نشر على Alibea.
- قبل تجهيز Alibea حُفظت واجهتها الحالية في `/opt/backups/accounting-app/alibea-s3-3b-before-20260906-162743` بصلاحية `0700`. تضم 183 ملفاً واجتازت المقارنة الثنائية الكاملة. تطابقت بصمة `index.html` للطرفين `dbcaa66e7888bacd60be7f459924e628984148eafc3dfc07244e4ff93aa8a116` وبصمة الأصل `/assets/index-JGMdg96S.js` للطرفين `292929623f39ef66befb9d36c663f46c72ce6f21d220ccfd65a5fc2760f1a6a2`. لم يحدث بناء أو نشر.
- بُنيت Alibea من الالتزام النهائي داخل `/tmp/s3-3b-alibea-build-20260906-163000` باستخدام lockfile الجديد والحزم المثبتة منه ومتغيرات Alibea الصريحة. نجح Vite بعد تحويل `3936` وحدة خلال `41.94s` وأنتج 183 ملفاً.
- الأصل الرئيسي الجاهز هو `/assets/index-CmoYbaBh.js` ببصمة `8c2139a17e7c0f6321b0234fa8e67f7a849046f9210cc14c1e11614c856aa4ba`، وبصمة `index.html` هي `d3700a9392d163b17ffa547fe22b93643066a008f887dd607e5a31c4020d3c83`.
- اجتاز البناء حواجز Alibea: احتوى API الخاص بها ومسارات `/assets/` من جذر النطاق، ولم يحتو API الخاص بـFarida أو مشروع Staging أو مشروع Lovable القديم. بقيت Alibea الحية مطابقة لنسخة الرجوع، وبقيت بصمات Farida وStaging المعتمدة كما هي. أعادت النطاقات الثلاثة HTTP 200 وبقي Nginx وDocker نشطين. لم يُنشر البناء بعد.
- نُشر البناء المتحقق منه على `/var/www/alibea` وحده باستخدام تحديث مؤجل وحذف أصول Alibea القديمة ذات البصمات. تطابقت الملفات الحية ثنائياً مع البناء (183 ملفاً)، وطابقت استجابات HTTPS للصفحة والأصل الرئيسي بصمتي `d3700a9392d163b17ffa547fe22b93643066a008f887dd607e5a31c4020d3c83` و`8c2139a17e7c0f6321b0234fa8e67f7a849046f9210cc14c1e11614c856aa4ba`.
- أكد الفحص الحي وجود API الخاص بـAlibea وغياب عناوين Farida وStaging وLovable. أعادت الصفحة الرئيسية و`/auth` على Alibea وواجهتا Farida وStaging HTTP 200، وبقي Nginx وDocker نشطين. ظلت بصمات Farida وStaging المعتمدة مطابقة، ولم تتغير قاعدة بيانات أو Edge Function.
- نجح أول اختبار وظيفي على Alibea بعد `Ctrl+F5`: حُمّلت الواجهة، ونجح تسجيل دخول المدير، وفتحت لوحة التحكم بصورة طبيعية.
- نجح التنقل إلى «التقارير ← تقرير المبيعات»، وفتحت الصفحة وعرضت بيانات Alibea الصحيحة دون صفحة بيضاء أو خطأ.
- نجح تبديل الفترة السريعة بين «هذا الشهر» و«هذا الربع»؛ تغير نطاق التاريخ وبيانات Alibea بصورة طبيعية دون تعليق أو خطأ.
- نجح تصدير PDF من تقرير Alibea، ونُزل الملف وفُتح وظهر التقرير داخله بصورة صحيحة دون خطأ.
- نجح تصدير Excel من تقرير Alibea، ونُزل الملف وفُتح وظهرت بيانات التقرير داخله بصورة سليمة.
- نجح اختبار `next=/reports/sales` بحساب المدير على Alibea بعد تحديث React Router إلى `6.30.6`، وانتقل إلى تقرير المبيعات داخل نطاق Alibea كما هو متوقع.
- نجح اختبار `next=/\\evil.example` على Alibea: رُفض المسار وبقي المستخدم داخل `alibea.alibea2020.com` وانتقل إلى لوحة التحكم دون فتح نطاق خارجي. بذلك اعتُمدت S3-3B وظيفياً على Alibea وأُغلقت على البيئات الثلاث.
- اجتاز فحص الإغلاق النطاقات الثلاثة بحالة HTTP 200 مع تطابق بصمات الصفحات والأصول المعتمدة، وبقاء Nginx وDocker نشطين وجميع حاويات الشركتين عاملة؛ الحاويات الموسومة health كانت `healthy`.
- أظهر الخادم نحو `2.5 GiB` ذاكرة متاحة، لكن Swap المستخدم بلغ قرابة `2.0 GiB`. أثبت `vmstat` خلال خمس قراءات عدم وجود `si/so`، ولم توجد أحداث OOM منذ ترقية Node. أكبر مستهلكين هما VS Code extension host (نحو 938 MiB) وTypeScript Server (نحو 541 MiB)، وليس خدمات الإنتاج؛ لذلك لا يوجد ضغط Swap نشط أو دليل عدم استقرار. لم تُعد تشغيل جلسة VS Code ولم يُفرغ Swap أثناء العمل.
- حُذفت 11 وجهة مؤقتة أنشأتها S3-3B حصراً من `/tmp` بعد الاعتماد، بإجمالي يقارب 946 MiB: محاكاة lockfile ونسخة التحقق وأبنية Staging/Farida/Alibea وذاكرة npm المؤقتة وتقارير audit المؤقتة. تحقق غياب جميع الأهداف بعد الحذف.
- بقيت نسخ الرجوع الثلاث تحت `/opt/backups/accounting-app` ولم تُمس، وكلها بصلاحية `0700`: `staging-s3-3b-before-20260906-153857` و`farida-s3-3b-before-20260906-155138` و`alibea-s3-3b-before-20260906-162743`. بقي القرص عند استخدام 25% ونحو 118 GiB متاحة.
