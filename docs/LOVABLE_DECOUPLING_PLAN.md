# خطة الانفصال الكامل عن Lovable

> تاريخ الإنشاء: 2026-09-06  
> الحالة: اكتملت L0 وL1 وL2 داخل cleanroom؛ لم تبدأ L3 أو تتغير أي بيئة
> نقطة الأساس: الفرع `main` عند الالتزام `3f63708`

## الهدف

إزالة الاعتماد التشغيلي والبنائي على Lovable، ثم إنشاء مستودع GitHub جديد بتاريخ نظيف، من دون تغيير مصدر نشر الإنتاج الحالي أو قواعد بيانات Farida وAlibea قبل نجاح دورة Staging كاملة واعتماد المستخدم.

المقصود بالانفصال هو ألا يحتاج التثبيت أو البناء أو المصادقة أو الترحيلات أو النشر إلى نطاق أو خدمة أو حساب تابع لـLovable. لا تُحذف الإشارات التاريخية التي تلزم لفهم أصل البيانات أو قرارات الترحيل قبل نقل نسخة مختصرة ومدققة منها إلى سجل أرشيفي مناسب.

## نتيجة الحصر الأولي

| المجال | الموجود حالياً | الأثر | القرار المبدئي |
|---|---|---|---|
| تثبيت الحزم | `package-lock.json` يحتوي 111 رابط `resolved` إلى مخزن Lovable | قد يفشل `npm ci` خارج بيئة Lovable؛ npm 12 رفضها فعلاً بـ`EALLOWREMOTE` | استبدال المصدر بـnpm الرسمي مع تثبيت الإصدارات و`integrity` ثم اختبار نظيف |
| مصادقة الواجهة | `src/integrations/supabase/client.ts` يستورد `previewAuthStorage.ts` الذي يتواصل مع نطاقات محرر Lovable | لا يعمل المسار الخاص على نطاقاتنا، لكنه كود تابع للمنصة داخل حزمة التطبيق | استبداله بتخزين مصادقة محلي مستقل واختبار الدخول/التحديث/الخروج وتعدد التبويبات |
| إعداد Drizzle | `drizzle.config.ts` يقرأ `LOVABLE_DB_MIGRATION_URL` | اسم متغير مرتبط بالمنصة وقد يربك التشغيل المستقل | الانتقال إلى `DATABASE_URL` مع فحص السكربتات والوثائق وعدم تضمين أسرار |
| تتبع الترحيلات | سكربتا النشر يستخدمان `public.lovable_schema_migrations` | الاسم موجود فعلياً في قواعد الإنتاج؛ تغييره المباشر قد يعيد تنفيذ migrations أو يكسر التتبع | ترحيل منفصل ومحمي إلى اسم محايد بعد نسخ السجلات والتحقق؛ يمنع الحذف المباشر |
| Migration قديم | تعليق في migration بتاريخ 2026-04-19 يذكر Lovable Cloud | تاريخي فقط ولا يغير التنفيذ | عدم تعديل migration مطبق؛ توضيح سياقه في سجل الترحيل إن لزم |
| ملفات الإرشاد | إشارات في `LOCAL_SETUP_GUIDE.md` و`ROADMAP.md` و`PRODUCTION_READINESS_AUDIT.md` وخطط التتبع | بعضها تعليمات قديمة وبعضها سجل تدقيق | تحديث التعليمات الحية، وحفظ الوقائع التاريخية بوضوح دون تقديمها كاعتماد حالي |
| Git | `origin` الحالي هو `creative-flame-keeper.git` والفرع `main` نظيف | تغييره على checkout الإنتاج قد يؤثر في `git pull` داخل سكربتات التحديث | إبقاؤه ثابتاً حتى اكتمال المستودع الجديد وStaging؛ لا force-push ولا استبدال مبكر |
| ملفات المنصة | `.gitignore` يستبعد `.lovable/`، ولا يوجد مجلد `.lovable` متعقب حالياً | لا أثر تشغيلي حالي | إزالة قاعدة التجاهل عند إنهاء التنظيف بعد التأكد من غياب الملفات |

## مستوى التعقيد والخطر

- التعقيد: متوسط؛ أغلب العمل في lockfile والمصادقة والتوثيق، بينما انتقال جدول تتبع الترحيلات يحتاج عناية أعلى.
- خطر التنظيف داخل مساحة مستقلة: منخفض جداً، لأنه لا يغير الملفات التي تخدم النطاقات ولا قواعد البيانات.
- خطر إنشاء مستودع جديد: منخفض، ما دام `origin` الحالي وسكربتات الإنتاج لم تتغير.
- خطر نشر الواجهة المنظفة: منخفض إلى متوسط ويعالج بنشر Staging أولاً ونسخة رجوع وحواجز API.
- خطر تغيير جدول تتبع الترحيلات: متوسط إلى مرتفع إن نُفذ مباشرة؛ لذلك هو دفعة قاعدة بيانات مستقلة بعد نسخة احتياطية وdry-run والتحقق على Staging.

## ضوابط غير قابلة للتجاوز

1. لا يُنفذ التنظيف داخل checkout الإنتاج `/opt/accounting-app` قبل نجاحه في مساحة مستقلة.
2. لا تتغير قواعد Farida أو Alibea خلال تنظيف الملفات أو إنشاء GitHub الجديد.
3. لا تُحذف migration مطبقة ولا يُعاد ترتيب تاريخ migrations.
4. لا يُعاد إنشاء lockfile عشوائياً؛ تثبت الإصدارات الحالية ثم تُغير المصادر فقط، وبعدها تطبق ترقية Vite المحددة كتغيير مستقل قابل للمراجعة.
5. لا تُنسخ ملفات `.env` أو مفاتيح أو نسخ قواعد أو `node_modules` أو `dist` أو مجلدات النسخ الاحتياطية إلى المستودع الجديد.
6. لا يُغير `origin` في checkout الإنتاج قبل اعتماد Staging ووجود نسخة رجوع واختبار أمر التحديث الجديد دون تنفيذ نشر.
7. لا يُستخدم `npm audit fix --force`.
8. كل دفعة لها فرق ملفات محدود، فحوص مستقلة، توثيق، والتزام Git منفصل.

## مراحل التنفيذ

### L0 — تثبيت خط الأساس ومساحة التنظيف

- إنشاء مساحة دائمة مستقلة خارج `/opt/accounting-app` وبلا `.git` من المستودع القديم.
- تسجيل بصمات `package.json` وlockfile والإعدادات وبصمات أصول Staging وFarida وAlibea.
- إعداد قائمة سماح للملفات المنقولة وقائمة منع للأسرار والمخرجات والنسخ الاحتياطية.
- لا نشر ولا تغيير remote في هذه المرحلة.

بوابة القبول: checkout الإنتاج نظيف، البيئات الثلاث HTTP 200، والخدمات والبصمات الحالية لم تتغير.

### L1 — فصل سجل الحزم

- تحويل كل `resolved` تابع لمخزن Lovable إلى `https://registry.npmjs.org/` مع إبقاء الاسم والإصدار و`integrity`.
- تشغيل `npm ci` نظيف عبر npm حديث، ثم فحص عدم بقاء أي نطاق Lovable في lockfile أو npm config المشروع.
- تشغيل التدقيق الكامل والإنتاجي ومقارنة شجرة الحزم قبل/بعد لمنع تحديثات عرضية.

بوابة القبول: صفر روابط Lovable، تثبيت نظيف، ولا اختلاف إصدارات غير مصرح به.

### L2 — فصل مصادقة المعاينة

- إزالة `previewAuthStorage.ts` وربطه من عميل Supabase، أو استبداله بوحدة محايدة إذا كانت هناك حاجة سلوكية مثبتة.
- الحفاظ على إعدادات Supabase الحالية لكل بيئة من متغيراتها الصريحة.
- اختبار تسجيل الدخول، استمرار الجلسة بعد refresh، تسجيل الخروج، انتهاء الجلسة، تعدد التبويبات، ودوري المدير والبائع على Staging.

بوابة القبول: لا نطاقات أو رسائل `postMessage` تابعة لـLovable داخل الحزمة، وكل اختبارات المصادقة ناجحة.

### L3 — تحييد أدوات قاعدة البيانات

- استبدال `LOVABLE_DB_MIGRATION_URL` باسم محايد مثل `DATABASE_URL` في الإعدادات والتعليمات، دون تسجيل قيمة سرية.
- تصميم migration مستقل ينشئ جدول تتبع محايداً وينسخ سجلات الجدول القديم دون فقد أو تكرار.
- تجربة migration وdry-run على Staging أولاً، والتحقق من أن جميع migrations تعتبر مطبقة ولا يعاد أي منها.
- يبقى الجدول القديم دون حذف حتى اعتماد الشركتين ومرور فترة رجوع متفق عليها.

بوابة القبول: تطابق سجل الترحيلات القديم والجديد، وdry-run بلا migrations قديمة معلقة، وعدم تغير بيانات الأعمال.

### L4 — تنظيف السكربتات والوثائق والهوية

- تحديث سكربتات النشر والترحيل لاستخدام الأسماء المحايدة بعد نجاح L3.
- إزالة تعليمات الربط بـLovable من دليل الإعداد وتحديث خريطة المشروع.
- إبقاء الوقائع التاريخية الضرورية في سجل واضح، مع حذف التعليمات المنتهية أو الملفات الترويجية/المولدة إن وجدت.
- فحص نصي شامل، مع تصنيف أي ذكر متبقٍ هل هو تاريخ مطلوب أم اعتماد يجب إزالته.

بوابة القبول: صفر اعتماد تشغيلي أو بنائي على Lovable، وأي ذكر متبقٍ تاريخي ومبرر صراحة.

### L5 — بوابات الجودة والأمن

- TypeScript.
- 48 ملف اختبار و563 اختباراً على الأقل.
- بناء production ومعاينة محلية وdev smoke test.
- `npm audit` كامل وإنتاجي، وفحص أسرار وتوكنات وعناوين قواعد الإنتاج داخل الملفات المتعقبة والبناء.
- بناء Staging بمتغيراته فقط والتحقق من غياب عناوين Farida وAlibea وأي عنوان Lovable.
- اختبار وظيفي يدوي للمصادقة والصلاحيات والمبيعات والمشتريات والمصروفات والتقارير والتصدير.

بوابة القبول: كل الفحوص ناجحة أو أن أي دين قديم موثق ومثبت أنه مطابق لخط الأساس وليس تراجعاً.

### L6 — مستودع GitHub الجديد

- إنشاء مستودع خاص جديد وفارغ باسم يحدده المستخدم.
- إنشاء تاريخ Git جديد من قائمة الملفات المنظفة، بدلاً من دفع تاريخ المستودع القديم بما قد يحويه من آثار أو أسرار قديمة.
- تنفيذ فحص أسرار أخير قبل الالتزام الأول، ثم دفع الفرع `main` وحمايته.
- إبقاء المستودع القديم أرشيفاً خاصاً وعدم حذفه أثناء الانتقال.

بوابة القبول: clone جديد ثم `npm ci` والفحوص والبناء ينجحون دون وصول إلى Lovable أو ملفات خارج المستودع.

### L7 — Staging ثم الإنتاج والرجوع

- ربط Staging أولاً بالمستودع الجديد أو نشر بنائه يدوياً، مع نسخة رجوع وبصمات وحواجز API.
- بعد اعتماد المستخدم، تغيير مصدر checkout الإنتاج في نافذة مستقلة مع اختبار fetch/dry-run قبل pull أو build.
- نشر Farida ثم اختبارها، وبعد الاعتماد نشر Alibea؛ لا نشر متزامن للشركتين.
- عدم تعديل قواعد البيانات ضمن دفعة الواجهة؛ ترحيل جدول التتبع له نافذة منفصلة.

بوابة القبول: النطاقات الثلاثة سليمة، البصمات موثقة، Nginx وDocker وقواعد البيانات مستقرة، وخطة الرجوع مجربة.

## خطة الرجوع

- يظل `/opt/accounting-app` وremote الحالي بلا تغيير حتى L7.
- تحفظ نسخة نشر لكل نطاق قبل استبدال الملفات.
- عند فشل الواجهة: استعادة مجلد النشر السابق وإعادة تحميل Nginx بعد `nginx -t`.
- عند فشل ترحيل تتبع migrations: إيقاف الدفعة وعدم حذف الجدول القديم؛ استخدامه كمصدر الحقيقة حتى تحليل السبب.
- لا rollback لبيانات أعمال عبر Git، ولا حذف لمستودع أو قاعدة أو جدول ضمن خطة الرجوع الآلية.

## تنفيذ L0 وخط الأساس — 2026-09-06

- أُنشئت مساحة التنظيف الدائمة `/opt/accounting-app-cleanroom` بملكية `deploy:deploy` وصلاحية `0750`، ونُقلت إليها الملفات المتعقبة فقط دون `.git` أو `node_modules` أو `dist` أو `coverage` أو `supabase/.temp`.
- تطابقت checksums الملفات المنقولة مع الالتزام `80ab2e6ceb418d00cba636fcccca865a9f5af7c5`، وبقي checkout الإنتاج نظيفاً ومتطابقاً مع `origin/main` عند إنشاء المساحة.
- كشف فحص أسماء الملفات أن `.env` متعقب في المستودع الحالي. لم يُقرأ محتواه ولم يُعرض؛ حُذفت نسخته من مساحة التنظيف فقط، وبقي ملف checkout الإنتاج دون تغيير. يمنع إدراجه في المستودع الجديد، وتراجع قيمه لاحقاً بفحص أسرار لا يطبعها قبل تقرير الحاجة إلى تدويرها.
- قائمة المنع للمستودع الجديد: `.git`، `.env` و`.env.*`، المفاتيح `*.pem` و`*.key`، ملفات service-role والتوكنات، dumps ونسخ قواعد البيانات، مجلدات النسخ الاحتياطية، `node_modules`، `dist`، `coverage`، `supabase/.temp`، logs وملفات المحررات. لا يسمح بقالب `.env.example` إلا بعد مراجعته والتأكد أنه أسماء متغيرات وقيم وهمية فقط.
- شُدد `.gitignore` داخل cleanroom فقط ليمنع ملفات البيئة و`.npmrc` و`.netrc` والمفاتيح وملفات credentials/secrets ونسخ قواعد البيانات والمخرجات والحزم وبيانات التشغيل. أزيل منه بند `.lovable/` وتعليقه كي لا يخفي عودة ملفات المنصة مستقبلاً؛ تأكد عدم بقاء اسم Lovable في الملف. لم يتغير `.gitignore` في checkout الإنتاج.
- أُضيف `scripts/check-repository-safety.sh` إلى cleanroom؛ يفحص أسماء الملفات الحساسة وأنماط المفاتيح المعروفة محلياً، ولا يطبع القيمة عند الاشتباه. كشف أول تشغيل أربعة JWTs لمفاتيح Supabase العامة: مفتاحا Farida وAlibea داخل `scripts/deploy-all.sh` وتكرارهما في `docs/DEPLOYMENT_WORKFLOW.md`.
- أزيلت القيم الأربع من cleanroom فقط. أصبح سكربت النشر يطلب `FARIDA_SUPABASE_PUBLISHABLE_KEY` و`ALIBEA_SUPABASE_PUBLISHABLE_KEY` من بيئة النشر ويفشل قبل نشر الشركة عند غياب متغيرها، وأصبحت الوثيقة تستخدم أسماء المتغيرات بلا قيم. اجتاز Bash syntax ثم اجتاز cleanroom فحص الأسرار بلا نتائج. حُذفت ملفات التحرير المؤقتة المحددة من `/tmp` بعد تثبيت النسخ النظيفة.
- لم تُعدل النسخ التشغيلية من `scripts/deploy-all.sh` أو `docs/DEPLOYMENT_WORKFLOW.md` في checkout الإنتاج ضمن هذه الدفعة؛ سيأتي المستودع الجديد من cleanroom النظيف، بينما يبقى المصدر الحالي ثابتاً حتى اكتمال التحقق.

### بصمات ملفات البناء المرجعية

| الملف | SHA-256 |
|---|---|
| `package.json` | `529d244013ec82e9f47cad1586efaf46921e0594206fbb53a1dc66f03f640c91` |
| `package-lock.json` | `cca7687a37b83e70be963113d4fdb5436b86f5bc8952287a9871bb89d6526d5c` |
| `vite.config.ts` | `aef2deda85c5c704ce6989a916e06d1831fc920adf08349f661bc6ba76b01641` |
| `vitest.config.ts` | `2c64550d05cdec49d120f28afd53056372945035b13d54cbd16f8027329f2a84` |

### بصمات النشر المرجعية

| البيئة | `index.html` | الأصل الرئيسي | SHA-256 للأصل |
|---|---|---|---|
| Staging | `f86be6682086cb363d0de838f158d8a3c43859b123f3b86a28f178cdd0465440` | `/assets/index-DeU0Y8Mc.js` | `eee96a4a927d10fb20477fea8731bf0b5c5725f5bffb95ad0bd79b4846ca940f` |
| Farida | `81b00744793d06a2641ef6b2380dec5a9eb56e8ef29418b88173653fa4cda388` | `/assets/index-JPiGv4kC.js` | `822e13108224e6bf738f853aa632aebeb41d94303c94e67495794aec212bf37d` |
| Alibea | `d3700a9392d163b17ffa547fe22b93643066a008f887dd607e5a31c4020d3c83` | `/assets/index-CmoYbaBh.js` | `8c2139a17e7c0f6321b0234fa8e67f7a849046f9210cc14c1e11614c856aa4ba` |

- أعادت النطاقات الثلاثة HTTP `200` و`text/html` وقت التسجيل. كان Nginx وDocker بحالة `active`، وجميع حاويات Farida وAlibea عاملة، وكل حاوية لها health check ظاهرة بحالة `healthy`.
- لم تتغير ملفات الويب أو إعدادات Nginx أو Docker أو قواعد البيانات أو remote أثناء L0.

## تنفيذ L1 وفصل سجل الحزم — 2026-09-07

- استُبدلت 111 قيمة `resolved` تابعة لمخزن Lovable داخل lockfile في cleanroom بنظائرها على `https://registry.npmjs.org/`. أثبتت مقارنة JSON أن هذا كان التغيير الوحيد أولاً، دون تغيير اسم أو إصدار أو `integrity`.
- كشف npm 12 أن lockfile القديم ناقص بيانات حزم SWC وRollup الاختيارية لأنظمة التشغيل الأخرى. أكمل lockfile بإضافة 33 تابعاً اختيارياً بالإصدارات نفسها وإزالة `yaml@2.9.0` الاختياري غير المطلوب، مع صفر تغييرات إصدار. أضيف `resolved` و`integrity` الرسميان لإدخالي `@swc/core@1.15.46` و`esbuild@0.21.5` الرئيسيين.
- سُجلت سياسة npm 12: رفض سكربت `core-js` والسماح بسكربتي `@swc/core` و`esbuild`. يسجل npm السماح بالاسم بسبب سلوك `install-scripts`، بينما يثبت `npm ci` الإصدار وSHA-512 عبر lockfile؛ لذلك يمنع تغيير lockfile دون مراجعة فرق مستقلة.
- نجح `npm ci` من الصفر من npm الرسمي: 651 حزمة، بلا رابط Lovable وبلا تدخل يدوي أو سكربت غير مراجع. عمل SWC `1.15.46` وesbuild `0.21.5`، واجتاز ماسح الأسرار بلا نتائج.
- لأن `.env` لم يُنقل، كشفت أول دورة اختبار اعتماد سبعة ملفات على متغيرات Supabase. أضيفت قيم وهمية محصورة في `vitest.config.ts` (`supabase-test.invalid` ومفتاح واضح أنه اختباري)، فلا تتصل الاختبارات بقاعدة حقيقية ولا تدخل القيم بناء الإنتاج. بعدها نجح TypeScript و48/48 ملف اختبار و563/563 اختباراً.
- نجح بناء Vite 5 المرجعي بقيم وهمية صريحة: 3936 وحدة خلال 58.37 ثانية، و183 ملفاً بحجم 7,312,389 بايت. بصمة `index.html` هي `1b00c1f98fdb6e3727e6a4c46dcd52c62fcaafef1536638c7941b09e6f644162`، والأصل `/assets/index-BAzPacr4.js` ببصمة `6a5e3dd1c9cb90cc46ce36cced4daba1a94187d166d3aa266fc5dc2b3d812c08`.
- احتوى البناء قيم الاختبار فقط، ولم يحتو نطاق Farida أو Alibea أو Staging ولا عنوان `supabase.co`. بقيت كلمة `lovable` وحدها بسبب `previewAuthStorage.ts` المستخدم حالياً؛ إزالتها هي L2 ولا تمنع إغلاق فصل سجل الحزم L1.
- نتيجة التدقيق بقيت خط الأساس المتوقع قبل ترقية Vite: 8 متوسطة و1 عالية. لا `audit fix --force`؛ ترقية Vite تبقى دفعة مستقلة بعد إزالة الاعتماد التشغيلي على Lovable.

### بصمات L1 داخل cleanroom

| الملف | SHA-256 |
|---|---|
| `package.json` | `5fec4868863008025b6012c7092b3df52c7d69a455c3f813de60a8b37270f843` |
| `package-lock.json` | `b7178f424149236d658c25dbf74b3426b70724494f50aa328e052afa6bd66983` |
| `vitest.config.ts` | `250a8dd7cf7f3a2c6fc6407b690569bb7d6f6d3a0f02900165d1cc61e2e2e2b4` |
| `scripts/deploy-all.sh` | `cb5600e7ecb62a6ebbf5f260d30af6a02e02891ecd9d6f6ff3143ca16bf4af85` |
| `scripts/check-repository-safety.sh` | `8459bca37cbac51c95af6f4d6f576fd95d3923f0cc68c6db69548a470eb67877` |

## تنفيذ L2 وفصل مصادقة المعاينة — 2026-09-07

- أثبتت مراجعة `previewAuthStorage.ts` أنه وسيط خاص بنطاقات معاينة Lovable فقط، وأنه يعيد `localStorage` مباشرة على نطاقات Farida وAlibea وStaging. كما أثبتت مراجعة Supabase JS `2.110.8` أن التخزين الافتراضي مع `persistSession: true` يستخدم `localStorage` في المتصفح ويوفر مزامنة التبويبات عبر `BroadcastChannel`.
- ثُبت عقد السلوك القديم أولاً بثلاثة اختبارات ناجحة لاستمرار قيمة الجلسة واستعادتها وإزالتها خارج نطاقات Lovable. بعد ذلك حُذف `previewAuthStorage.ts` واستيراده وخيار `storage` من عميل Supabase داخل cleanroom فقط.
- أضيف اختبار يمنع إعادة وسيط تخزين خاص بمنصة إلى إعداد عميل Supabase، واختبار تكاملي يثبت أن التخزين الافتراضي لـSupabase يستخدم `localStorage` ويستمر ويستعيد ويحذف قيمة الجلسة. نجح الاختباران المستهدفان، ثم نجح TypeScript بلا أخطاء، ثم نجح كامل الاختبارات: 50/50 ملفاً و565/565 اختباراً.
- اجتاز ماسح الأسرار بلا نتائج. اختفت إشارات Lovable من كود الواجهة؛ بقي اسم متغير Drizzle واسم جدول تتبع migrations في سكربتين فقط، وهما ضمن L3 ولا يجوز تغيير جدول التتبع مباشرة.
- نجح بناء Vite 5 بقيم Supabase وهمية: 3935 وحدة، و183 ملفاً بحجم 7,310,399 بايت. لم تحتو الحزمة Lovable أو نطاقات Farida أو Alibea أو Staging أو أي مشروع Supabase حقيقي، واحتوت قيم الاختبار الوهمية فقط.
- بصمة `index.html` هي `a4d263f2ce2926d390adb2918e10a2fd3fd667e34d370a6769e06232dc84fa1e`، والأصل الرئيسي `/assets/index-J1s2QPsP.js` ببصمة `0d8f74e63b12f361bd4846d6919af8d225030f75661bac15ef0fbf3fef968727`.
- أكدت مقارنة checksum مع المصدر أن الفروق محصورة في L0 وL1 وL2. لم تُنقل `.env` أو بيانات ربط Supabase أو بيانات المحرر أو ملفات TypeScript المؤقتة، ولم يحدث نشر أو تغيير قاعدة بيانات أو تعديل كود في checkout الإنتاج.

### بصمات L2 داخل cleanroom

| الملف | SHA-256 |
|---|---|
| `src/integrations/supabase/client.ts` | `ba148436047edc5c5e624aa5513e9fb1ab76f7a4a48b5fb9efa45267a1398872` |
| `src/integrations/supabase/client.test.ts` | `a8e9ece909ae790834273595b02c390053d0df0b134493ef7c8097ae22c4b33c` |
| `src/integrations/supabase/defaultAuthStorage.test.ts` | `3c2f8aa9849ea30e6a42c7b8ad19fb66a270034221fe3bc99ac14c6ef1b993a0` |
| `src/integrations/supabase/previewAuthStorage.ts` | محذوف من cleanroom |

## الحالة الحالية والخطوة التالية

- مكتمل: L0 وL1 وL2؛ مساحة cleanroom محمية، وسجل الحزم ومصادقة الواجهة منفصلان، والتثبيت وTypeScript و565 اختباراً والبناء ناجحة.
- لم يبدأ: L3 لتنظيف إعدادات قاعدة البيانات والترحيلات، أو أي تغيير remote أو قاعدة بيانات أو نشر.
- الخطوة التالية بعد الموافقة: استبدال `LOVABLE_DB_MIGRATION_URL` باسم `DATABASE_URL` داخل cleanroom فقط، ثم تصميم ترحيل آمن لاسم جدول تتبع migrations دون تطبيقه على أي قاعدة.
